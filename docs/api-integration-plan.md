# C-H5 真实接口接入与承接计划

> 本文只维护当前有效状态、已验收能力、剩余缺口和下一步。接口路径、字段和页面调用详情见 [Swagger 匹配矩阵](./api-swagger-match-matrix.md)，开发规则见 [AGENTS.md](../AGENTS.md)。历史开发过程、旧 Swagger 快照和测试过程以 Git 记录为准，不在本文重复追加。

## 当前承接状态（2026-08-29）

| 口径 | 当前状态 |
|---|---|
| 分支 | `main`；准确提交位置以 Git `HEAD` 为准 |
| 结论 | **部分完成**；H5 主交易、账户、资金读取、隔离履约、仅退款、求购审核、IM 图片/语音和 KYC 提交已完成真实回归，剩余工作集中在 KYC 私有资源地址、断线运行时、资金受控写入与缺失契约 |
| 实施进度 | 约 `94%–96%`；仅作当前能力分层估计，不能替代逐项验收 |
| 真实回归度 | 约 `86%–90%`；按真实请求、页面展示、响应及刷新/详情回读统计，后端阻塞项仍计入未回归，真机验收按当前任务排除 |
| 主链路 | 真实地址、商品、购物车、合并下单、订单组付款、买手发货、顾客签收和仅退款已完成真实回归 |
| 本批完成 | C-PC 页签图标同步；KYC 文件上传、`fileId` 提交和签名地址刷新；carrier 发货、采购单号、采购/发货凭证上传、物流详情、卖家轨迹/异常入口；求购审核状态/地址/驳回展示与参考图上传；商品详情卖家/分类真实字段；订单群兼容 `IM_MESSAGE`、`IM_READ`、`IM_RECALL`，已改用真实 `realUserId` 判断本人消息，以 Long 安全、单调阅读水位线展示他人已读状态；断线恢复后按服务端 `GET /im/messages/incr` 增量补偿，补偿失败时保留待补偿标记以便后续重连重试；消息首页与通知详情在实时事件到达后刷新未读数和摘要，消息首页以本机 Socket 状态展示连接结果而非全局在线数；图片/语音收发播放与实时状态更新；WebSocket 按 `apiPrefixPath/gatewayPath` 与 `tokenSources` 自适应连接并遵守服务端心跳上限；通知精确跳转；积分规则/VIP 配置字段补齐；商品删除与充值取消；新版移动端视觉替换已覆盖页面结构、图标与安全区，已恢复首页业务模块、商品详情轮播、分类树层级与双栏独立滚动、购物车操作，并统一原生顶部导航白色背景；首页商品区与“我的”资产区已移除未经真机验收的 CSS Grid，改为等价 flex 换行布局；2026-08-29 已完成两件隔离商品审核上架：第一件完成跨账号支付、凭证发货、物流轨迹、买家回读与确认收货闭环；第二件完成支付、仅退款申请、详情回读和撤销；隔离订单群的 IM 图片已完成上传、发送、卖家回读和撤回态回读；隔离求购已完成参考图上传、创建、后台驳回及 C 端审核意见回读；订单、理财、售后列表未登录直接访问触发真实请求的问题已修复为先初始化用户并在无会话时安全退出；50 条页面路由访客态全量复核无空白页、无控制台 error/warn 或未处理异常；新增受保护详情/列表页登录守卫覆盖求购、评价、订单、售后、理财、钱包、IM、买手商品与收藏，未登录直达不再发出真实请求；收藏页改为 `onShow` 刷新，完成收藏→列表回读→取消收藏闭环；以测试账号通过 H5 页面创建并撤销隔离求购，顾客提交评价、买手回复及买手申诉均完成接口与页面回读 |
| 本轮追加 | 评价删除、通知单条删除与通知清空已在测试环境完成页面确认和空态回读；订单群新建文字消息后立即撤回，刷新页面仍显示“消息已撤回”；Chrome 本地 H5 完成购物车删除确认取消、地址新增底部弹层关闭、求购分类 ActionSheet 选择、商品费用说明弹窗、帮助文章与协议底部弹层的打开/关闭回归。发现帮助中心 `wd-collapse` 未绑定 `v-model` 导致分类无法展开，已补充受控展开状态并验证文章弹层可打开关闭；求购“全部/待审核/推送中/已接单/已驳回/已取消”和售后“全部/待审核/已同意/已驳回/已撤销”页签均完成本地 H5 切换，无控制台 error/warn；订单群真实会话页面显示实时连接可用，页面重载后历史消息、图片/语音入口和消息撤回态保持，控制台无 error/warn。当前 Chrome 能力不提供网络离线/强制关闭 Socket 的控制入口，因此断线增量补偿仅保留代码与接口证据，不冒充运行时通过。本轮两个同源 Chrome 标签共用 storage，不将该标签复核作为独立双账号证据；既有独立双账号实时证据继续沿用 |
| 前端稳定性排查 | 买手保证金、订单/物流、消息通知和评价时间字段已兼容数字时间戳、数字字符串与标准日期字符串，保证金流水不再显示 `Invalid Date` 且排序保持正确；订单、仅退款、求购、评价、理财持仓页签增加请求序列门禁，快速切换时只接纳最后一次响应；订单、售后、评价、通知、结算和理财的读取/写入失败补齐可见错误态与失败提示，不再将接口失败伪装成空数据或产生未处理 Promise。旧 Mock 订单群路由仅由未使用的演示组件引用，真实入口仍全部进入真实订单群，按既有迁移边界保留未删除 |
| 本轮稳定性回归 | Chrome iPhone 15 Pro 尺寸复用登录态完成保证金流水、订单、仅退款、求购、评价、理财产品/持仓、结算、消息与通知读取；保证金两条数字字符串时间显示为有效本地时间，订单/售后/求购/评价页签连续快速切换后最终数据与所选页签一致，结算页真实地址、商品快照、余额与金额正常，全部页面无新增控制台 error。`pnpm typecheck`、`pnpm build:h5`、`pnpm build:app-plus` 与 `git diff --check` 均通过 |
| 列表错误态补充 | 商品综合列表、买手商品、分类树/分类商品、收藏、积分、钱包流水及充值/提现记录已区分加载中、真实空数据和首次加载失败；分页追加失败时保留既有真实数据。分类首屏新增树加载态，避免接口尚未返回时短暂误显示“该分类暂无商品”。Chrome iPhone 15 Pro 尺寸已回归分类非空商品、公开商品非空列表、收藏空态、积分非空流水和钱包非空流水，均无控制台 error |
| 本轮素材验证 | 新建隔离 QA 账号后，以合成、明确标注非真实证件的 PNG 完成 KYC 三张影像上传、签名地址换取和 `PENDING` 提交回读；合成 1 秒 M4A 完成 IM_VOICE 上传、发送、公开音频资源回读及 H5 语音入口播放检查。KYC 签名地址当前指向不可达的 `127.0.0.1:8000`，资源 GET/页面图片展示保留为后端地址配置阻塞 |
| 最近检查 | 2026-08-29 已通过 `pnpm typecheck`、`pnpm build:h5`、`pnpm build:app-plus` 和 `git diff --check`；newUI 变更范围未发现浏览器专属运行时或未经真机验证的 CSS Grid。公开测试环境的分类、推荐、热销、新品和商品分页均实测返回成功且非空，横幅与秒杀为真实空数组；Chrome iPhone 15 Pro 尺寸已确认登录、首页、分类、商品列表与“我的”访客态正常渲染：首页推荐商品双列 flex 布局和原生 tabBar 正常，页面均无控制台告警；商品列表关键词搜索返回真实匹配数据，不匹配时正确显示空态；真实商品详情的价格、运费税费、库存、卖家和评价读取正常；购物车访客空态正常，结算页按预期重定向登录且未发生写入；50 条页面路由逐页新标签复核均无空白页、控制台 error/warn 或未处理异常，仅结算页按预期重定向登录。终端测试账号的账户、积分、钱包、资金流水、买卖订单、订单详情、通知、IM 会话、历史消息和 `GET /im/messages/incr` 均成功读取，订单详情首条请求曾出现一次临时 `code=-1`，换用另一真实订单后正常返回。Chrome 自动化对商品列表“最新”筛选的点击在输入派发阶段超时两次，未产生页面控制台错误，记录为自动化限制；此前 2026-08-27 已复核补偿失败不清除待补偿标记的实现并再次通过 `pnpm typecheck` 与 `git diff --check`；Notify Swagger 与状态接口可读，`/api/notify/im` 已实测返回 `101` 并回显 `im` 子协议；H5 测试账号登录后消息中心真实数据读取、页面重载和服务端在线连接回读均通过（`onlineConnections: 1`）；订单群历史消息、撤回渲染、已读上报和实时 ready 无前端错误；双账号 QA 文本已验证无刷新 `IM_MESSAGE` 到达、`IM_READ` 发送方显示“已读 1”，并立即撤回后双端实时替换为“消息已撤回”，断线增量补偿仍待运行时验证；本轮新增 H5 登录态逐页读取我的、钱包、订单、评价、通知、IM、求购、KYC 等页面，john/mamba 均无控制台 error/warn；收藏页刷新 bug 已以收藏→列表→取消回读验证；求购创建/撤销、评价提交/回复/申诉/删除、通知删除/清空和订单群文字发送/即时撤回刷新回读均通过（当前两个同源 Chrome 标签共用 storage，不将本轮标签复核作为独立双账号证据）；新增本地 H5 弹窗回归覆盖购物车删除取消、地址弹层、求购分类选择、费用说明、帮助文章和协议，帮助中心折叠 v-model 缺失已修复并通过同一组检查 |
| 当前阻塞 | KYC 历史详情因既有后端文件已删除不可读取，页面已降级保留“已通过”状态；新隔离 QA 账号的 KYC 上传、签名地址接口和 `PENDING` 提交已成功，但签名 URL 指向不可达的 `127.0.0.1:8000`，实际资源 GET/页面影像展示仍受后端存储地址配置影响；IM 图片/合成语音已完成真实回归；测试账号的 ETH、TRON、BSC 专属充值地址均返回后端 `Empty key`，无法创建 `PENDING` 充值单以验证取消；订单群断线增量补偿尚待运行时回归，当前 Chrome 能力无法注入离线或强制关闭 Socket；CMS、AI 仍无可替换的 C 端契约。帮助中心折叠交互已由前端修复并通过本地回归，不再计为阻塞 |
| 当前服务异常 | 无新增前端可复现的详情服务异常。已确认订单列表卡片显示的是订单号，点击详情时透传订单实体 ID；此前把显示订单号手工当作 `id` 请求详情导致失败，不属于后端详情服务异常。从真实列表点击进入后，订单详情、金额、物流摘要、凭证与轨迹均成功读取 |
| 下一步 | 前端可独立推进的公开/登录态页面、隔离求购/评价/通知/IM 写回归及本轮 KYC/语音素材验证已收口；后续仅在条件出现时继续验证 KYC 存储地址、订单群断线增量补偿、充值链地址及后台状态分支。真机验收按当前任务明确跳过，CMS/AI 仍等待有效 C 端契约 |

## 全局交互整改目标（2026-08-30，持续推进中）

本轮按代码审查的 16 组问题执行整改，状态为**部分完成**。下方既有梯队百分比属于此前主流程回归基线，不能证明本轮异常分支、角色边界和大数据分页已验收。本次按用户要求保存并推送阶段性代码与任务文档，准确提交以 Git 为准；原有 `backend-remediation.md` 不属于本轮修改，不随本次提交。顶部“当前承接状态”按项目规则保持不变。

本次阶段性保存边界：

- 交易、消息、会话隔离、分页、地址及多类写操作恢复的累计代码随本次保存；求购、评价等正常写回读证据见下方，未验收异常分支不视为完成。
- KYC 已接入按账号持久保存的原提交摘要/文件 ID/前次申请标识，明确区分未知、已收到 ID、已核对；不会保存原证件号或私有签名 URL，脱敏号码仅按实时契约进行展示值核对，不声称证明完整证件号相同。提交前复核最新状态，未知不重发，原申请核对后且被驳回或过期才允许重新填写。身份证/护照切换清理全部旧影像，并隔离上传中的类型往返变化；上传/提交期间锁定表单。
- KYC 类型切换 7 组源码断言、评价恢复 13 组源码断言已通过；KYC 创建恢复尚缺专项故障验证及真实提交/审核/驳回重提验收，不能据此关闭创建恢复整组。下一步从 KYC 恢复专项验证继续，再推进商品/收藏、履约恢复、深链接、AI 展示及其余真实异常回归；真机仍排除。

| 批次 | 对应审查项 | 当前实现与剩余验证 |
|---|---|---|
| 交易正确性 | 1–4、16 | 已补订单双方 ID/数量/商品小计、详情及操作入口角色守卫、整组待付款分页回读与金额确认、付款后状态回读；结算按账号/上下文保存原请求及幂等键，已付订单恢复不受余额阻挡；库存刷新、数量限制、失效选中项拦截、支付结果页真实状态已实现。提现/申购/充值的成功回执与后续读取分离，网络结果不明时跨刷新保存本机防重标记并提供记录核对入口；保证金保留原金额、原幂等键恢复。待真实异常写链路验收，前端保护不等同服务端原子保障 |
| 消息可靠性 | 5、6、14 | 文字/图片/语音按原 clientMsgId 重试，媒体同时复用原 mediaFileId、不重复上传；响应合并与失败消息保留。取消录音/页面隐藏/卸载丢弃未发送素材，选图/录音/发送互斥；可见底部已读串行合并并保留失败水位。补偿按断线代次隔离，旧请求完成不清掉新断线游标，支持 500 条分批失败续接；Socket 合并并发启动、忽略过期启动、重复 READY 不叠加心跳、最后一个订阅页面退出才释放。上述竞态均有内存复现与修复断言，仍待双账号、真实媒体、断线和大消息量运行时回归 |
| 导航和数据完整性 | 8–13 | 已补独立地址选择、订单快捷筛选/身份统计刷新；订单、售后、求购、评价、持仓、保证金、会话、通知分页及商品/收藏触底修复；求购分类筛选继续扫描后页，评价资格不再限首 50 条；相关返回页与个人/KYC 状态刷新已补。通知统一分类，交易入口进入交易通知，读取全部未读页；求购详情按真实 buyerId 判断发起人，不再套用查看者 ID。大数据及登录态交互仍待验收 |
| 全局状态和表单 | 7、15 | 会话切换统一清理用户/钱包/Socket，旧请求 401 不清理新账号，网络失败保留凭据并允许资料重试；失效登录携带安全业务回跳参数。KYC、求购、评价、发货、商品发布增加上传/提交屏障，资金地址/金额、求购天数、商品库存增加有效性校验；退款、买手申请、积分申诉、保证金增加重复提交保护。待实际连续操作与会话切换回归 |

当前证据：

- live Swagger 已递归核对订单详情/创建/组付款/商品详情、提现/充值/申购/赎回及 IM 消息分页/增量/发送/已读相关 schema。组付款仅接收 `orderGroupNo`，不支持服务端原子校验预期金额；前端确认前后回读不能消除付款瞬间并发调价风险。提现、充值、申购当前未提供幂等键，未知结果不能盲目重发。
- 截至本批全部代码修改，最终 `pnpm typecheck`、`pnpm build:h5`、`pnpm build:app-plus`、`git diff --check` 均通过；新增代码未引入浏览器专属运行时或禁止的布局能力。App 构建不代表真机验收，真机仍排除。
- 不落盘的内存断言验证了十进制金额计算、Long ID/角色/数量、多上下文保留、同键重试、零余额已付款恢复、账号隔离、消息失败重试/合并、隐藏/阅读历史不主动已读及取消/隐藏不发送录音；商品与收藏列表均验证连续触底仅请求一次、请求成功才提交页码、失败后重试同页。此类断言不计作真实接口回归。
- 测试账号真实读取 3 笔买入及 3 笔卖出订单，角色映射核对通过；无订单参数直达支付结果页，Chrome 移动视口显示“支付结果 / 付款尚未确认”，无控制台 error/warn。
- 新增内存断言通过：统一分页并发/筛选过期响应/失败重试、旧会话 401 隔离/当前会话清理/断网初始化重试、资金未知结果持久化与账号隔离、保证金同金额同键恢复、通知跨页分类，以及订单总数以数字字符串返回的兼容。未新增单元测试文件或测试依赖。
- 消息并发补充断言通过：补偿期间再次断线不会越过缺口，旧补偿期间新 READY 会继续补偿新边界；500 条后读取失败保留游标；同水位重复事件只发一个已读请求，新水位串行跟进，失败/隐藏不错误推进。图片和语音发送失败各验证原 clientMsgId/mediaFileId 重试且上传次数保持一次；取消录音无上传。Socket 并发进入仅建一条连接，停止后的旧启动不能复活，页面退出不关闭其他订阅者的连接。已付款恢复等待钱包刷新期间离页/切换账号，不再被迟到响应强制跳回结果页。这些均为内存执行生产代码的断言，不算浏览器/真实网络回归。
- 新增真实只读证据：求购共 4 条，抽取 3 条详情核对发起人；全部未读通知 8 条，与独立未读总数接口一致；评价 2 条、会话 18 条；持仓和待评价订单均为空，不能据此认定其非空/大分页回归完成。Chrome 交易通知分类标题和售后列表访客态正常渲染，不算登录态业务验收。
- 交叉审查补充修复：基础登录资料只依赖 `/auth/me`，不再因积分/VIP服务异常而失败；个人中心 VIP 按当前顾客/买手维度读取，缺失积分/等级不补成 0/VIP0。立即购买增加入口锁及页面代次校验；取消收藏后重建第一页并隔离旧分页响应/旧账号确认；买手在售统计传 `ON_SALE`；语音实时撤回及历史合并撤回均停止对应播放。内存执行源码验证登录请求范围、缺失字段、购买并发、21 条收藏删除边界、统计筛选及播放器状态通过，不计为真实回归。
- 购物车隔离补充：按账号/游客分别保存商品、选择状态和立即购买上下文；游客主动结算使用 `guestTransferId` 交接本次选中项，不带入账号原有选中商品，交接记录绑定接收账号且重复打开不重复合并。游客立即购买上下文只迁移一次。无账号标识的旧版购物车保留原始存储，在购物车提供明确恢复入口，新增恢复项默认不勾选，不自动归属任意账号。保存失败回滚内存；旧账号商品刷新、旧确认弹窗和旧地址响应不能改写新会话；结算加载/交接失败禁止提交，付款后清理失败保留下单进度供核对恢复。
- 本批不落盘断言通过：A/B/游客隔离、游客交接幂等与跨账号拒绝、立即购买单次迁移、旧版恢复不覆盖同款且保留原记录、存储失败回滚、旧账号异步刷新拒绝、结算交接失败拦截及地址/协议/待恢复记录的会话清理。Chrome 重新加载后游客购物车为空，未自动显示旧版商品；首次仅切换 hash 时仍是热更新保留的旧 Store 状态，不作为新代码验收。登录态跨账号/交接/旧版恢复真实点击尚未完成，不能将内存断言或游客空态算作这些场景通过。
- 消息与通知补充：消息首页各模块独立落地结果，摘要或分类计数失败不隐藏三个业务入口；初始化后校验页面代次，卸载后不再创建迟到订阅。通知实时事件只提示有更新并刷新计数，不重置历史列表；手动重载失败保留已加载记录和页码，重试回到实际失败页。已读、全部已读、删除和清空绑定账号/页面并互斥，旧计数响应不能覆盖写入结果；删除后重建分页，资料初始化失败可原地重试并恢复实时订阅。公共分页保留原默认行为，仅通知开启保留式重载。
- 搜索补充：输入草稿与已提交查询快照分离，追加分页及下拉刷新不读取未确认关键词；确认搜索/排序重新从第一页开始，首次或追加失败均重试原页，离页旧响应不回写。内存执行源码通过：60 条通知历史保留、刷新失败与第一页重试、分类继续翻页、重复已读单次请求、过期计数/旧账号弹窗/离页弹窗拒绝、删除回读失败恢复、公共分页默认行为保持，以及搜索草稿/分页/刷新/失败重试隔离；这些不计为真实接口回归。
- 表单所属与回执恢复补充：KYC、商品发布、求购创建、评价和退款申请使用页面/会话操作保护；切账号或卸载清空本页资料，普通异步回写与延迟导航在离页后失效，原生选图允许同一会话返回原栈页。评价/求购/商品发布逐张上传前后检查归属，不使用跨页面全局上传遮罩。同一会话隐藏期间收到成功回执时保留已提交状态和结果入口，不自动跳转、不因导航取消允许重提。评价及退款补返回资格刷新和失败重试；求购加载失败、无效分类或失效地址在提交函数内同样拦截。
- KYC 防重恢复：只在回读到本次申请的新驳回时解除防重；同 ID 重提后的旧驳回记录不解锁，需审核时间/提交时间变化或已观察到本次待审核状态。护照提交不带身份证反面文件。商品发布先读取当前买手与 KYC 资格，无资格不上传或创建；分类/资格失败支持重试，正常返回保留草稿并重新读取，过期分类不能提交。
- 本批生产源码内存断言通过：评价第二页 Long ID 资格、正常相册返回（含回调先于 onShow）、旧账号选图/上传/弹窗拒绝、离页停止后续上传、旧操作 finally 不解锁新操作、重复提交、隐藏成功回执、延迟导航取消、退款旧详情拒绝、KYC 新旧驳回区分、私有资源迟到回写拒绝、发布资格门禁/分类失败恢复、求购加载及地址门禁。初次 KYC 断言没有等待 onShow 内异步加载结束，修正验证时序后通过；未新增测试文件、依赖或接口字段。这些不是实际上传、审核、退款或多账号浏览器回归。
- Chrome 本批已读取商品发布访客态的资格提示和登录弹窗；点击可见“取消”仍在 `Input.dispatchMouseEvent` 阶段超时，未完成真实取消、登录及表单写链路验证。复用现有 5174 服务，不因浏览器输入问题重启服务或替换真实数据。
- 账户表单补充：提现、积分申诉、买手申请已清理切账号后的本页输入、回执与弹窗；提现从弹窗打开起互斥，确认前比较原金额/链/地址并检查当前余额，余额加载失败禁止提交，未登录或无效/缺失余额显示未知。无幂等创建的本机防重保护仍保留未知结果记录；解除保护的旧弹窗在离页后失效，旧账号请求 finally 不会解锁新账号正在执行的请求。该公共保护不等于其他资金页面的全部生命周期问题已解决。
- 申请与回读补充：买手申请正常返回保留未提交草稿，成功回执不受回读失败影响；本次新驳回才能重新提交，同 ID 旧驳回不能解锁。积分申诉记录本次返回的申诉 ID，旧流水不会重新开放提交，读取到该申诉的审核终态后可解除本页防重。求购详情抢单/撤销的锁覆盖弹窗、写入和回读；成功后回读失败或仍返回旧可操作状态时保留成功标记，仅开放刷新，返回或切账号的过期回调不再执行旧动作。
- 本批针对性生产源码断言通过：提现弹窗并发、参数变化、账号/离页失效、成功隐藏回执、未知结果持久化、旧确认不清除保护、旧请求不解锁新请求及余额未知展示；买手申请草稿保留、回读失败保留成功、新旧驳回区分；积分旧弹窗/旧列表拒绝、重复提交、精确申诉 ID 终态恢复；求购回读期间锁、失败/旧状态锁、成功终态回读及隐藏回执。Chrome 提现页访客表单与登录提示可读取，真实提现、申诉、申请审核及求购写操作未执行；浏览器输入超时仍沿用最近一次已确认的工具边界，不计为业务通过。
- 商品管理补充：列表与详情共用状态/实际 sellerId 能力判断，沿用既有列表的非在售删除范围，不因浏览身份变化授权商品操作；最终未完结订单及权限限制仍由后端裁定。上下架/删除从确认弹窗开始互斥，写前回读详情并核对原状态和归属；账号/页面/筛选变化后不执行旧动作。上下架成功但回读失败或仍为旧状态时保留待回读标记，详情已确认新状态而列表仍旧时使用真实详情记录；删除成功的本页记录不因旧列表回读重新出现，隐藏期间收到删除回执不强制导航，返回异常不改报删除失败。
- 商品管理验证：生产源码内存断言通过五种已声明状态/非本人商品/未知状态、双入口重复点击、旧账号/旧页面弹窗、写前状态与归属变化、失败/旧状态回读、隐藏删除、旧 finally 不解锁新操作、筛选变化，以及 51 条分页连续加载/失败原页重试/删除后重建第一页保留移位商品。最终类型检查、H5/App 构建和 diff 检查通过；Chrome 完整重载后确认商品管理访客显示“请先登录查看商品”，不再将未读取数据显示为“暂无商品”。初次重载仍见旧文案，确认开发服务已编译新分支后再次加载取得新页面证据；不将热更新旧视图算作新代码通过。未执行真实上下架、商品删除或登录态权限回归。
- Chrome 本批公开搜索读取到 3 条真实匹配商品，消息首页访客态三个入口正常显示；排序点击仍在输入派发阶段超时，未完成输入编辑/连续翻页及通知登录态真实操作，不算相关交互已验收。
- Chrome 两种点击方式均在 `Input.dispatchMouseEvent` 阶段超时，登录态点击回归尚未完成；保留本地服务并继续代码和独立验证，不将该控制异常记作业务缺陷。未执行真实付款、提现、申购、赎回或消息写入。

- 首页/分类/VIP 补充：首页按模块独立落地结果，点击重试仅请求失败模块，返回页面重新读取；消息红点绑定通知与 IM 真实未读计数，未知/失败不冒充有未读，实时事件合并刷新，离页释放订阅与计时器。秒杀按有效结束时间过滤活动商品，已结束/时间未知不再显示进行中，到期最多对同一过期集合补读一次，不因后端仍回旧场次而循环请求。现有字段没有开始时间，本批不虚构未开始状态。分类树及商品支持明确失败重试，页码成功后推进、追加失败保留列表、同分类刷新失败保留原数据，离页旧结果不回写。VIP 页签只改变本地权益预览，任何用户均可查看买手权益，不切换业务身份；积分/等级缺失不补零/VIP0，账户和权益配置独立读取，支持失败重试及返回刷新。本批最终 `pnpm typecheck`、`pnpm build:h5`、`pnpm build:app-plus` 和 `git diff --check` 通过，未提交或推送。
- 本批验证：生产源码内存断言覆盖 VIP 本地视角/未知等级与真实零积分/独立失败/会话清理/离页返回，分类树失败恢复/Long ID/分页原页重试/旧响应拒绝，首页单模块重试/未读零值与失败/实时事件合并/隐藏清理，以及秒杀结束、无效时间和单次到期刷新。Chrome 复用 5174 服务读到首页推荐/热销/新品、分类非空真实商品，以及 VIP 访客的两个权益页签与未知积分。上述 DOM 读取不证明真实点击、跨账号、非空未读或活动到期已验收；本批未新增测试文件、依赖、API 或接口字段，未重新读取 Swagger。初次类型检查发现 Socket 回调 unknown 类型已修正。

- 订单列表/发货补充：确认弹窗打开即互斥，取消/收货在写前回读原订单状态与实际归属，旧页面/会话/筛选确认不继续；列表读取与上传均隔离迟到结果。发货图片绑定弹层代次、原账号/订单及原栈页，相册返回可继续，关闭或离页不追加旧凭证，移除跨页面全局上传遮罩。发货参数在提交时冻结，写前再次核对订单；成功回执与列表回读分离，旧列表不能重新开放同状态写操作；网络结果不明单独提示核对并阻止本页重复发货，不能据此宣称跨刷新/跨设备幂等。订单卡操作区同步禁用；分页失败保留记录、重试原页；访客显示登录或重试入口。
- 订单批次验证：生产源码内存断言通过重复弹窗、隐藏/切账号/切筛选失效、写前状态变化、成功后失败/旧状态回读、原生选图返回/迟到上传/弹层关闭、旧 finally 不解锁新账号、发货隐藏成功回执、业务拒绝后草稿重试、未知结果不伪成功及 31 条分页边界。Chrome 已读取订单列表访客提示和五个页签；真实取消/收货/上传/发货及登录态未执行。本批仅复用既有接口，没有改变请求契约或读取新 Swagger，也没有新增测试文件。

- 订单详情/物流补充：详情返回重新读取，失败保留同账号已有数据并禁止写入，切账号清理订单/物流/表单与回执；取消/收货从弹窗打开起互斥，写前核对原 ID、实际归属和状态，隐藏或会话变化后的确认不提交。详情付款/取消/收货成功后保留本页回执，旧状态回读不重新开放动作。物流轨迹/异常提交冻结原表单，关闭弹层、离页或切账号使写前异步失效；业务拒绝保留草稿，隐藏成功保留回执而不弹提示，未知结果显示核对入口而非成功。轨迹按返回 ID 回读，未知结果按原时间/状态/说明/位置/异常节点组合核对；异常按本次目标内容回读，未知结果不凭其他无关字段变化解除同内容旧状态保护。这些仍是本页保护，不等于跨刷新/跨设备幂等已实现。
- 详情批次源码验证通过：同账号失败保留/跨账号清理、旧弹窗及重复点击、预检状态变化、旧成功状态锁、物流弹层关闭、准确 Long 回执/相似轨迹不解锁、隐藏成功、业务拒绝草稿恢复、未知结果及原请求回读。最终类型检查、H5/App 构建与 diff 检查通过；复用 Chrome 和 5174 服务核对详情访客入口，真实订单/物流写入及独立账号回读仍未执行。未新增测试文件、依赖或 API 参数，未重新核对实时 Swagger，未提交或推送。

- 申购/登录/结算导航补充：申购从确认起互斥，重新读取产品条件和余额，金额/期限/利率等确认内容变化时重新确认；余额未知不显示零，返回保留未提交金额，切账号清空表单和回执，隐藏成功不跳转且可手动查看持仓，沿用持久化未知结果防重。登录取消成功后的延迟定时跳转；旧页面或已切换会话的登录响应不写入新凭据，内部登录封装携带同次资料与会话回执，Store 再次校验后建立身份，辅助资料迟到不能恢复旧账号；登录已成立但离页的情况保留可手动继续入口。结算失效返回绑定原页面/会话/加载代次，隐藏、卸载、切账号和重新加载取消旧定时器，无上一页时回购物车。
- 本批生产源码断言通过申购草稿/未知余额/弹窗互斥/金额与产品条件变化/余额读取失败/隐藏成功/旧 finally/持久化防重，登录页面无延迟跳转、密码清理、隐藏回执、API 旧结果拒绝及 Store 会话绑定，结算定时器取消和直接进入兜底。Chrome 申购访客页与登录提示已读取；未进行真实申购、登录切换或资金写入。没有新增测试文件、依赖或 HTTP 契约字段，登录封装返回结构仅在本项目 API 与唯一 Store 调用方间调整；未重新核对 Swagger，未提交或推送。

- 充值申报/轮询补充：确认弹窗打开即互斥，冻结金额和链，写前重新读取开放链并核对最低金额/标签变化；旧页面、旧账号和参数变化不继续创建。专属地址按链、会话及读取代次隔离，离页清除地址，失败和链不匹配不展示旧地址。返回保留草稿并刷新配置；创建成功回执独立于详情回读，隐藏成功不自动导航，失败可原地刷新或手动进入本次记录。轮询由每次读取结束后再计时，手动刷新与轮询互斥，隐藏/卸载/切账号停止，旧请求不能恢复计时器或覆盖新一轮结果；未知创建结果沿用持久化本机防重。
- 充值详情补充：返回重新读取并校验 Long ID，读取失败保留同账号旧数据、显示重试并禁止取消；切账号清理详情。取消从确认开始互斥，写前回读状态；成功回执不被回读失败或旧 PENDING 状态抹去，网络结果未知/回执不匹配保留待核对提示，隐藏成功不弹提示。取消保护仍为本页范围，不等于跨页面、跨刷新或后端幂等已验收。本批最终 `pnpm typecheck`、`pnpm build:h5`、`pnpm build:app-plus` 与 `git diff --check` 通过。
- 本批生产源码断言通过充值草稿保留、旧确认拒绝、写前条件变化、地址账号/链隔离、成功回读失败恢复、隐藏成功及显式查看、串行轮询/返回代次/终态停止、旧 finally 不解锁新操作、创建未知结果持久化，以及取消状态复核、成功旧状态锁、未知回执与业务拒绝区分。初次取消业务拒绝断言使用了错误的 RequestError 构造方式，按项目真实构造参数修正后通过；未因此修改业务错误分类。Chrome 技能复用现有 5174 服务读取充值页和详情页访客登录/重试提示；没有执行真实申报、取消或链上转账。本批未新增测试文件、依赖、接口或请求字段，未重新读取 Swagger，未提交或推送。

- 公共登录守卫补充：23 个现有调用页面统一在 setup 创建 `useNavigationGuards`，不在异步回调内注册生命周期。登录初始化、提示弹窗与回跳绑定原会话/栈页/显示代次；离页后再返回同一页仍拒绝旧确认，同页重复提示合并且保留首个回跳地址，旧弹窗结束不清除新提示。已登录返回 true、未登录立即返回 false 的调用约定不变，网络失败保留凭据、不误开登录弹窗。买手守卫复用登录判断，身份回读失效不切换视角，KYC 延迟跳转可取消并复核当前资格；当前没有 requireBuyer 业务调用方，本批只修正公共能力，不宣称买手页面全面接入该守卫。
- 公共守卫验证：生产源码断言通过重复/取消/失败提示、回跳参数保留、离页返回/卸载/会话重置、迟到初始化、旧 finally、买手权限/视角切换结果及 KYC 定时器取消。申购、充值申报和充值详情的异常分支使用真实守卫封装重新执行通过；类型检查、H5/App 构建及 diff 检查通过。Chrome 技能复用现有服务核对充值访客登录提示；没有执行真实登录、账号切换或 KYC 跳转点击，不计为完整浏览器验收。未新增测试文件、依赖或接口契约，未提交/推送；页面自身业务读取及写回执仍按后续任务继续核查。

- 保证金补充：本批实时读取 swagger-config 与 user 分组，递归核对 `/buyer/deposit/page`、`pay`、`refund` 的请求/响应及依赖 schema；确认缴纳/退还返回流水 ID、同键只生效一次，未改变 HTTP 契约。页面会话/弹层代次保护覆盖资格预检、提交和回执，隐藏/切账号关闭弹层，旧 finally 不解锁新账号；移除全局处理中遮罩。原金额/原键恢复不受钱包当前余额阻断，由后端校验实际可扣/可退部分。
- 保证金成功回执写入原账号的本机幂等记录，精确流水 ID、PAY/REFUND 类型及账号归属回读后才解除保护；旧流水/失败回读不开放新操作。存储成功回执失败时保留本页回执和原请求，清理失败可刷新重试；迟到业务拒绝不删除同键已确认回执。旧版原请求兼容恢复，格式损坏的本机记录不删除、不覆盖，也不阻止读取流水。余额缺失/读取失败/待成功回读时显示未知，文案明确为最近流水余额；翻页失败保留数据、重试原页，隐藏响应不回写。保证金成为第 24 个页面级登录守卫调用页。
- 本批源码断言通过原金额/原键恢复、成功跨页面实例恢复、精确回执/类型核对、存储失败和同键迟到错误、无效原记录保护、弹层关闭/离页/账号/金额/资格变化、旧 finally、隐藏成功、真实零与未知余额及 31 条分页边界。Chrome 技能复用 5174 服务读到保证金访客登录入口和 `U —`，没有执行真实缴纳、退还或链上资金操作；后端幂等语义来自契约核对，不以本机断言替代真实验收。未新增测试文件或依赖，未提交或推送。 初次类型检查发现可空回执访问，已修正；最终 `pnpm typecheck`、`pnpm build:h5`、`pnpm build:app-plus` 与 `git diff --check` 均通过。

- 理财赎回补充：本批重新读取 swagger-config/user，递归核对持仓 page/detail/redeem 及依赖 schema；确认详情仅本人可读、赎回返回原锁仓订单 ID，未声明客户端幂等键。本金及可得利息必须为有效值，确认弹窗展示产品/本金/净收益，确认后再次读取资格及金额条件；变化时不写入，要求重新确认。页面/会话/筛选代次隔离包括切走又切回原筛选，旧 finally 不解锁新账号操作。
- 赎回提交前按账号和原订单 ID 保存未知结果标记，成功回执、已核对终态同样持久化；刷新页面不自动重发。按原 ID 查询详情核对 REDEEMED/SETTLED/CANCELED，列表缺席、详情失败、相似/不匹配 ID、仍为 HOLDING 均不能解除保护；已核对终态仍保留防重记录，防止后续旧列表重开按钮。终态核对仅说明原订单不可再赎回，不能把自然结算或取消冒充赎回成功。业务拒绝可恢复，网络未知保持待核对；存储损坏阻止新写入但保留持仓读取。未知结果仍为 HOLDING 时不自动重发，需继续回读或后端核实原请求。
- 本批生产源码断言覆盖持久化成功/未知结果/跨页面实例恢复、确认前后条件变化、旧页面/账号/筛选 ABA、隐藏成功、业务拒绝、缺失/不匹配回执、终态后旧 HOLDING、存储失败/损坏、无效本金及 31 条失败原页重试。类型检查、H5/App 构建及 diff 检查通过；Chrome 技能复用现有服务读取持仓页访客登录入口，未执行真实赎回、收益到账或多端回读。持仓成为第 25 个页面级登录守卫调用页；无新增测试文件、依赖或接口字段，未提交或推送。

- 动态详情补充：提现详情由仅首次加载改为返回刷新，使用原 ID/会话/页面显示代次校验；同账号失败保留旧详情并提示刷新失败，切账号清理私有数据，旧请求不能覆盖新结果或结束新加载。读取成功后才接纳匹配 ID 的记录；访客显示登录/重试，复制成功提示绑定原页面，不在离页后提示。
- 付款结果补充：返回重新逐笔回读原订单 ID 集合，核对返回 ID、当前账号顾客归属与有效金额；部分请求失败、错 ID、非本人买入订单或无效金额都不显示付款已确认，也不展示未验证的结果摘要。读取中或失败时不沿用旧付款成功状态；账号变化清理结果，旧响应和旧 finally 隔离。主订单 ID 必须包含在参数集合内，回跳与查看订单继续保留 Long ID；本批沿用已有 20 笔参数上限，需继续核对其与实际下单组规模的一致性，不能据此宣称任意规模结果页已验收。
- 本批生产源码断言通过提现返回刷新/保留失败数据/错 ID/请求竞态/账号清理/复制回调，付款全量参数集合回读/十进制总额/状态更新/错 ID/非本人归属/无效金额/部分失败/旧页面/参数错误及访客分支。初次内存断言因验证适配层复制了依赖对象，未替换到预期读取函数，修正适配层后通过；未为通过断言改变业务逻辑。类型检查、H5/App 构建与 diff 检查通过；Chrome 技能读取提现访客登录提示，付款结果缺失参数不显示成功。未执行真实提现状态变更、付款或登录态交叉回读；未重新读取 Swagger、改动接口、新增测试文件或依赖，未提交或推送。

- 回跳与私有读取补充：登录失效回跳改为逐页面参数白名单，保留真实商品 source、付款完整 orderIds、搜索及结算上下文，过滤无关凭据参数；登录页兼容外层已解码/未解码参数，不二次解码目标查询值。钱包流水/充值记录/提现记录复用私有分页保护，初始化期间互斥、隐藏失效、会话清理、原失败页重试；流水弹窗离页及切账号清空。公共分页拒绝 null/空串/布尔/无效总数，数字字符串仍兼容；旧任务不结束新任务的下拉刷新。其他未接入隐藏失效的列表仍待处理。
- 小金库概览补充：账号变化清空资产/产品，返回重新读取，初始化失败不显示真实空态；产品与资产独立落地，失败保留同账号上次数据并标注重试，未知资产不补零。收益组件已移除固定公式生成的假走势及无真实数据的日/周/月/年筛选，保留真实累计收益和“暂无收益走势数据”，不新增 Mock 或走势接口。
- 求购共享接单回执的既有代码本批重新执行生产源码断言通过：大厅/详情跨入口保护、身份及条款预检、未知结果、精确关联订单归属核对、失败恢复和旧 finally 隔离；真实接单及跨账号回读仍未验收。付款结果上限 20 与当前 createBatchOrder 的 1–20 项校验一致，本批未重新读取 Swagger，不将本地对照作为新增契约验收证据。
- 本批验证：钱包三页覆盖 51 条分页/失败原页重试/初始化连点/会话清理/隐藏及旧响应，理财覆盖独立错误/真实零值/无效金额/过期响应；真实请求层回跳函数与登录页面断言覆盖 Long ID、20 笔集合、参数过滤及嵌套编码。类型检查、H5/App 构建及 diff 检查通过。Chrome 技能复用 5174 服务读取四页访客提示，不作为登录态非空列表、资金数据、会话切换或登录失效真实回归证据。未新增测试文件、依赖或 HTTP 参数，未提交/推送，未更新顶部承接状态。

- 退款双入口补充：实时 order 契约确认撤销仅限申请人且状态为 APPLYING，返回退款单 ID；卖方列表只读。类型补齐 buyerId/sellerId。详情先核对退款单与关联订单 ID、状态及双方归属，整组读取成功才更新；列表/详情使用同一账号级撤销回执，提交前保存未知标记，成功和核对终态持久化。隐藏成功不跨页提示，旧账号/页面/筛选确认失效，旧 APPLYING 与列表缺席不能重开撤销。业务拒绝、未知结果及审核进入其他终态分别处理，不将其他终态冒充撤销成功。
- 退款验证：生产源码断言覆盖详情原子更新、31 条分页/原失败页重试、旧弹窗、切账号/筛选、存储失败、缺失/错回执、跨入口恢复及旧 finally；类型/H5/App/diff 检查通过。Chrome 登录后读取 2 条已撤销退款记录，并打开其中一条详情，确认退款 ID、关联订单、金额及已撤销状态正常展示；未执行本轮真实创建、撤销或后台审核，这些异常写分支仍未验收。
- 付款回执补充：重新读取 swagger-config/order，递归核对 group/pay、bought/page、detail 及依赖 schema；付款只接收订单组号，返回本次付款笔数，未声明预期金额/版本、客户端幂等键或逐单付款的原子保障。确认前后核对完整待付款集合，保存原 Long ID 和规范化金额；分页拒绝无效总数，离页/换账号停止后续扫描。付款提交与详情读取分离，按账号/订单组共享 unknown、confirmed、verified 回执，覆盖结算、订单列表、订单详情；已发出的请求报错不自动重发，返回 0 笔不等于订单已付款，回执写后失败仍保留内存证据与原持久未知标记。
- 付款只读核对必须匹配原 ID 集合、组号、顾客归属、金额及所有已付款状态；部分成功、旧 CREATED、失败读取、错记录与超出确认范围的付款笔数均不解除保护。列表/详情同时阻止旧待付款记录上的付款及取消；结算保留原请求与购物车，全部订单回读通过后才清理和进入结果页。切账号/离页的迟到结果保存到原账号但不清理当前购物车、不跳转、不解锁新操作。本机防重不替代跨标签页/设备的后端原子保证；未知结果长期维持 CREATED 时，仍需服务端核实原请求。
- 付款验证：生产源码断言通过三个入口、完整集合/金额/归属、付款返回与查询失败分离、0/无效/超范围笔数、部分付款、网络/业务未知、同组互斥、存储失败/新实例恢复、旧确认与账号 ABA、隐藏成功、购物车清理失败保留及 51 条生产 API 分页逻辑；退款及提现/付款结果既有源码断言重新通过。最终 pnpm typecheck、pnpm build:h5、pnpm build:app-plus、git diff --check 通过；没有新增测试文件、依赖或 HTTP 参数。
- 浏览器条件更新：本批 Chrome 输入正常，使用项目本地记录的测试账号完成登录、返回订单列表；读到 7 笔买入订单（采购中/已完成），待付款筛选返回真实空态，已有采购中订单详情读取正常。旧的 Input.dispatchMouseEvent 超时不再作为当前阻塞；仍未执行本轮真实付款、超时注入、部分付款或跨账号回执回归，不能把登录/非空读取算作付款异常验收。未提交、推送或更新顶部承接状态。

- 评价列表补充：本批重新读取 swagger-config/order，递归核对 reviewable/mine/received 分页、detail、delete、reply、appeals/create 及 schema。回复限 500 字且每条仅一次，申诉理由限 512 字且同一评价只能有一笔待处理申诉；删除/回复返回评价 ID，申诉返回独立申诉 ID。无 HTTP 路径、请求字段或类型变更。
- 评价操作现在从打开弹窗开始互斥，确认后读取原评价详情，校验 reviewId/orderId、真实 userId/sellerId 归属、发布/回复/申诉状态；离页、账号/筛选切换及切走切回均使旧确认失效。回复/申诉失败和取消保留同账号当前页面草稿，重开编辑框预填；只有明确成功才清草稿，切账号/卸载清理内存输入，不把草稿写入日志或文档。
- 删除/回复/申诉的 unknown/confirmed/verified 回执按账号持久保存；已删除记录不会因旧列表重新出现，回复成功与列表刷新分离；申诉按新回执 ID 核对，旧驳回不能解锁，精确新申诉进入终态且仍满足资格后可再次申诉。未知结果不自动重发；回复详情出现已有回复只证明不能再回复，不冒充原请求成功；删除后的 404/列表缺席不证明未知删除已成功。存储失败、缺失/错回执、迟到成功和旧 finally 均有保护。
- 评价读取区分未登录、资料失败、列表失败和真实空态；失败保留同筛选旧数据并暂停写入，原失败页重试，隐藏使旧请求失效，筛选重建第一页。生产源码断言通过三类操作、草稿重填/长度、会话与筛选 ABA、写前归属变化、跨实例防重、新旧申诉 ID/终态、删除旧数据、存储失败、缺失回执、成功后刷新失败、未知删除 404 及 31 条分页边界；最终类型检查、H5/App 构建、diff 检查通过。Chrome 已读取待评价空态、两条已发布的“我发出的”评价，并打开/取消删除确认；未删除历史评价，真实回复/申诉及异常写回读仍待隔离数据回归。未新增测试文件或依赖，未提交/推送，未更新顶部承接状态。

- 求购撤销与筛选补充：已按本轮核对的 demands/my/page、detail、cancel 契约，保留撤销接口返回的原 Long ID，列表和详情共用按账号/求购 ID 持久保存的 unknown/confirmed/verified 回执；弹窗前互斥，确认后复核实际发起人、原 ID 和 PENDING_REVIEW/OPEN。只有精确回读 CANCELED 才确认撤销，VOID、TAKEN、REJECTED 不冒充撤销成功；存储失败、错回执、旧状态及跨入口不解除保护。列表区分访客/资料失败/真实空态，隐藏和筛选变更使旧任务失效；本地筛选每轮最多扫描 5 页，未完成时明确提示并可继续。生产源码断言重新通过双入口、账号/筛选 ABA、持久回执、存储失败及第 6 页匹配/原页重试；目前只读取历史已取消求购，尚未执行本批隔离创建/撤销写回归。
- IM 私有会话与恢复补充：本批重新读取 swagger-config/notify，递归核对 by-order、消息分页/增量/发送/已读/撤回、上传与依赖 schema；by-order 要求会话成员，分页第一页自动已读，send 按 clientMsgId 幂等，read/recall 返回布尔值。无 HTTP 参数、API 类型或接口变更。订单群初始化改为可重试，核对订单业务 ID、会话 ID 和成员角色后订阅；订单状态依照该会话实际角色读取，不跟随全局身份视角。
- IM 账号变化/卸载清理会话、历史、输入、媒体重试、阅读水位、补偿任务与订阅；选图/上传/发送/历史/增量/已读和播放器回调均绑定原账号与会话。隐藏使尚未完成的读取和确认失效，已发出的同会话发送结果允许保留但不触发隐藏页面滚动/已读；旧任务 finally 不释放新任务锁。录音 stop 未回调时不允许开始新段，防止旧录音归属到新账号；发送回执须匹配会话/本人/clientMsgId/消息类型，失败仍按原键重试。IM 会话列表接入已有私有分页保护，区分访客/资料失败/真实空态，刷新失败保留旧数据，隐藏失效和原页重试。
- IM 撤回从弹窗开始互斥，确认后复核当前页面/账号/会话及原消息归属与状态；只有严格 true 才应用成功态，false 不伪装成功，缺回执/网络结果未知时本页禁止重发并提供消息回读。实时撤回或精确消息回读能解除本页未知标记；本批未建立跨刷新持久撤回回执，仍作为后续未知操作恢复范围保留。
- IM 验证：生产源码断言通过初始化失败重试、会话错配、访客/资料失败、订阅唯一、隐藏与账号 ABA、旧发送/已读/分页/增量结果及 finally 隔离、相册往返/上传换账号、迟到录音 stop、严格撤回布尔回执、未知撤回回读、500 条补偿失败续接和补偿中再次 READY。测试脚本仅内存执行，未新增测试文件。Chrome 读取 18 条真实会话，进入隔离订单群（订单实体 ID 2093370220769533954，展示订单号 2093370220756951040），仅新发本轮 QA 文本并完成发送成功→取消撤回→确认撤回→重载后撤回态回读；没有撤回历史消息或改变订单状态。无参数入口明确显示缺少订单信息。源码断言不等于真实跨账号/断网/大分页/媒体竞态验收，这些继续保留；真机仍排除。

- 订单取消/收货共享回执补充：本批重新读取 swagger-config/order 的 cancel、confirm、detail 与依赖 schema。取消仅 CREATED、涉及回补库存；确认收货仅顾客 SHIPPED→COMPLETED、会触发卖家结算，两者返回原订单 ID。沿用现有顾客操作入口，不因契约支持卖家取消而扩展 UI。另核对 user/recharge/cancel/detail：仅本人 PENDING 可取消、已有链上到账不可取消、返回充值单 ID；充值已另设独立回执，不套用订单组或不存在的充值归属字段。
- 列表与详情已共用按账号/订单 ID/动作保存的 unknown/confirmed/verified 回执，提交前持久保存并读回，提交后保存失败保留更强内存证据；精确匹配原 ID、订单组和实际顾客。确认前加锁并复核页面/账号/筛选/原订单状态，取消和付款共用订单组锁，已存在付款回执不能取消，待核对取消回执也不能付款。缺失/错误 ID、HTTP/网络及服务端业务异常均保留未知（契约未声明库存/结算步骤原子化），仅请求层配置错误可证明未发送并解除本次标记。
- 取消/收货的已知成功只接受 CANCELED/COMPLETED 精确终态；未知取消若已进入其他非 CREATED 状态，只说明不能再取消，不宣称原取消成功；未知收货只在 COMPLETED/REFUNDED/CANCELED 时完成结果核对。验证后保留回执，旧 CREATED/SHIPPED 或与已确认终态矛盾的缓存不能重新开放写入。列表提供独立于当前筛选的原订单只读核对入口，详情进入/刷新核对，回执损坏暂停写入但保留详情读取。私有订单列表资料失败不再冒充空记录。
- 本批生产源码断言通过双动作、跨入口/新实例、原 ID/组号/顾客、已知成功冲突/旧状态、未知结果、存储失败、付款/取消弹窗互斥、旧页面/账号/筛选 ABA、隐藏成功；既有付款、结算、51 条整组分页断言重新通过。最终类型检查、H5/App 构建及 diff 检查通过，没有新增测试文件、依赖或 HTTP 字段。Chrome 使用已有 QA 商品新建本轮单件待付款订单：组号 2094007694093017088、实体 ID 2094007694130765826、展示订单号 2094007694118182912，金额 U 100；取消付款确认，没有执行付款。随后从订单列表打开/取消取消确认，再确认取消，列表回读已取消；详情跨入口和重载均显示“已核对：订单已取消”，没有再次付款/取消入口。取消后商品库存仍为回归前的 9；详情原先把空物流摘要显示为“待发货”，已修正为“订单已取消，未发货”并在页面回读。历史订单未变更，确认收货的真实结算、服务异常注入、跨账号及跨设备防重仍未验收。本机持久回执/应用实例锁不能替代后端跨设备幂等与原子保障。
- 本次实测保留边界：建单前页面已有历史“上次下单结果待确认”，未操作该记录；本轮已取消 QA 订单的结算清理见下方独立验收，不能将其推广为未知历史请求可删除。此前热更新期间两次直达/重试显示订单详情加载失败，同一订单从列表重新进入恢复；未取得底层错误，不归因于后端，保留直达失败原因复核。未提交、推送或更新顶部承接状态。
- 结算进度与取消收口：重新读取 swagger-config/order 的 create-batch、bought/page、detail 及递归 schema，确认同批每项一个订单、返回 ID 顺序对应原商品项、分页没有组号过滤。结算进度改用严格存储读写，校验原请求/幂等键/Long ID 集合/完整落盘快照，新旧记录冲突或读取失败不当成空记录；原请求、模式和购物车行不可被同键覆盖，删除需精确匹配并回读，旧版记录先迁移再移除副本。
- 已取消结算只在原账号/组号/完整订单集合、商品及数量全部匹配后收口：不带状态筛选扫描全部买入分页，校验总数稳定、无重复及缺页、同组无额外未取消记录；与付款共用组锁，有付款回执则保留。确认弹窗前后分别核对，只有全部 CANCELED 才清理该条本机进度，不清购物车/立即购买上下文、不删除订单、不创建或付款；历史未知和其他账号记录保留。
- 充值取消持久保护：本批重读 user/recharge/cancel/detail/schema，按账号/充值 ID 持久保存 unknown/confirmed/verified，确认后复核原链、金额、PENDING 与无 txHash，回执必须为原 ID。已知成功只接受 CANCELED 精确回读；未知结果若已 CONFIRMED，只表示到账、不能宣称取消成功。存储失败、错 ID/链/金额、旧 PENDING、离页或账号切换不重开取消；请求后存储失败保留更强回执。资料读取失败优先显示错误而不是登录空态。
- 本批检查：结算清理 6 组、充值取消 5 组生产源码边界断言，以及既有付款 7 组、结算付款 4 组、订单取消/收货 5 组断言通过，覆盖 51 条跨页、混合/多余/缺失记录、存储故障、旧弹窗、会话 ABA、原账号迟到结果与终态冲突。类型检查、H5/App 构建及 diff 检查通过；没有新增测试文件或依赖。Chrome 对原 QA 组 2094007694093017088 打开清理确认、取消后记录仍在，再确认后只移除该组，重新进入结算不恢复，历史未知记录仍在；原订单 2094007694130765826 详情仍为已取消。充值列表读到 4 条已到账记录，已到账详情无取消入口；充值页专属地址读取及重试仍失败，本批未创建申报、转账或取消历史记录。真实存储故障/跨账号/多页异常和充值取消写回读仍未验收，源码断言不代替真实异常验收。

- 地址管理隔离与创建恢复：本批重读 swagger-config/user 的 addresses/list/detail/create/default/delete 及递归 schema。list 返回本人完整地址（最多 20 条），首条新增自动默认；新增返回 Long ID，默认/删除为 Void 成功，删除默认地址后自动选择其他默认地址。无 HTTP 参数和 API 类型变更，不把 Void 成功错判为缺失 ID。
- 地址页接入既有页面/会话代次保护，读取失败保留旧列表但禁止操作；区分未登录、资料失败和真实空列表，提供登录/重试。旧长按菜单、离页/切账号、旧剪贴板和弹层回调失效；已发出请求的结果只归原账号保存，旧 finally 不释放新账号操作。结算选址回传绑定原页面会话及单次选择代次，可在原结算页隐藏时接收有效选择，但账号 ABA、卸载和重复回传被拒绝；不改变默认地址。
- 地址新增/设默认/删除使用按账号持久化的 unknown/confirmed/verified 回执，写前读取完整列表，默认/删除再核对原地址详情；存储失败不发送。新增保存原请求与原有 ID 集合；返回旧 ID/无 ID 或网络结果未知时不重发，只有唯一新增匹配记录可按“已观察到结果”恢复，既有同内容地址和多个新匹配不能冒充成功。默认/删除按完整列表核对，删除默认地址还需检查剩余默认状态；未知删除列表缺席只表示当前地址不在列表，不宣称原请求成功。成功与后续读取分离、旧状态不解锁、列表/回执损坏保留只读与重试。
- 地址验证：7 组生产源码断言及结算选址回传断言通过，覆盖完整列表/20 条限制、首条默认、空回执契约、旧/错 ID、新旧同内容记录、未知/成功回读、存储失败、旧弹窗/剪贴板/隐藏/账号 ABA、默认回补、独立选址与旧回传；既有结算付款 4 组及取消结算 6 组断言重新通过，类型/H5/App 构建及 diff 检查通过。Chrome 新建 QA 地址 2094016956324208642，验证新增回读、设为默认、恢复原默认，以及结算单独选择 QA 地址（默认仍为原地址）；未提交订单、未改历史未知结算。随后用本项目测试账号通过生产地址 API/操作逻辑删除仅该新建记录并精确回读，完整列表恢复为原来 1 条且原默认不变，再由 Chrome 页面回读。删除长按菜单真实点击、真实故障注入及跨账号仍未验收，不将 API 删除等同于菜单验收。未新增测试文件/依赖，未提交、推送或更新顶部承接。

- 退款创建与原订单保护：本批重读 swagger-config/order 的退款创建、买入分页、详情、撤销、确认收货及递归 schema。退款创建返回新退款 Long ID；分页只支持真实 orderNo 精确筛选，不支持 orderId 查询；创建没有幂等键。OrderView 单独保留真实 orderNo，不把可能回退成订单组号的展示 code 用作筛选；DTO 补充既有 refundType 字段，HTTP 路径与提交参数未扩展。
- 创建前保存账号/原订单/卖家/金额/原因/旧退款 ID 集合；存储失败不发送。已知回执按退款详情和关联订单核对，未知结果仅查找唯一新增匹配，并明确为“观察到匹配记录”，不冒充接口成功回执；旧同内容记录、多个匹配、错误归属/金额/类型、分页重复/总数变化均不能解除保护。撤销/驳回后需主动重新填写，旧申请迟到回读不得覆盖新申请；终态回退与本机终态冲突保持拦截。缺失金额明确报错，不补零。
- 确认收货/取消订单的公共提交入口，在确认前后核对原退款申请及撤销回执；退款结果未知时，旧 SHIPPED 快照不能解除保护。订单列表/详情展示原申请核对入口并暂停冲突操作，沿用按订单/组的实例内锁；已发送响应保存到原账号，隐藏成功不导航。本机保护不替代后端状态原子校验、跨设备幂等和未知结果查询能力。
- 退款验证：11 组生产源码断言通过，覆盖原 Long ID/orderNo、唯一新增/旧/缺席/多匹配、错误回执和关联、51 条完整分页、金额变化、写前/写后存储失败、新旧申请竞态、终态冲突、隐藏与账号切换、驳回重提、退款/收货互斥、双入口恢复，以及访客/资料失败、错误订单和账号 ABA 旧读取拒绝。相关订单变更 5 组、结算付款 4 组、取消结算 6 组、地址 7 组断言通过；最终 pnpm typecheck、build:h5、build:app-plus 与 git diff --check 通过。Chrome 读取 2 条历史已撤销退款；对本轮已取消 QA 订单 2094007694130765826 打开申请页，无提交入口，点击返回后读取 8 条买入订单。本批未对历史已付款订单、未知结算或资金执行写入；未新建已付款隔离单，真实退款创建/撤销/审核异常与冲突收货仍待验收，不把只读页面或源码断言算作写回归。未新增测试文件/依赖，未提交、推送或更新顶部承接。

- 商品发布恢复：本批重读 swagger-config/order 的 products/create、my/page、detail、files/upload 与删除契约及递归 schema。创建无幂等键，返回商品 Long ID；图片按 bucket/filePath 提交、详情返回有序公开 URL。新增账号级原请求/图片路径/提交前本人全量 ID 集合回执；不按状态或标题裁剪基线，避免旧商品改名或审核流转被误认新增。核对唯一新增匹配与详情的归属、分类、金额、库存、售后、文案及有序图片；未知/多个匹配/旧 ID/不完整分页不解锁，不把匹配观察冒充原接口回执。仅配置错误可确定未发送并解除，其他未知保留。
- 发布页重进先恢复原记录，错误态与已成功回执独立；核对成功后用户可主动“发布另一件商品”，只清本机本次记录并重置草稿，不删除服务器商品。旧账号/隐藏/卸载的响应只保存原账号回执，不迟到跳转；写前重新确认买手资格，原图与请求快照固定，库存按 int32 和图片 1–6 张校验，不额外保存签名 URL 参数。商品“待审核”页签仍保留 UI 的 PENDING，API 边界转换为实时契约要求的 REVIEWING，不增加页签。
- 真实回归发现并修复 BigDecimal 格式：首件 QA 商品已创建，但零运费/税费实际返回 `0E-8`，原金额工具拒绝指数表示，导致回执核对失败。公共金额工具改为字符串逐位展开科学计数法，保留大数和小额精度，不先转浮点；无效值及过大指数明确拒绝。ProductDTO 金额类型补充实际返回的字符串，不把格式差异当成订单或商品不匹配。
- 商品发布验证：12 组源码断言覆盖持久恢复、旧/缺席/多匹配、全部原字段与图片、51 条基线分页、资格变化、存储错误、迟到/隐藏/卸载/账号切换、显式新商品、PENDING 转换及 `0E-8`/小额指数/大数精确金额；相关退款 11 组、付款 7 组、结算付款 4 组、充值取消 5 组、地址 7 组断言通过。类型、H5/App 构建及 diff 检查通过，未新增测试文件或依赖。
- 本批真实证据：终端调用生产 API/恢复逻辑创建零库存 QA 商品 2094026061076062209，触发上述金额核对失败；修复后另建 2094026660756676610，完成创建回执 → 原详情精确核对 → REVIEWING 页签非空回读，再仅删除第二件并比对完整列表。Chrome 验证发布表单、第一件真实详情及删除确认，随后真实列表回到原 7 条，两件 QA 均不在列表；未上架、未产生订单、未动历史商品。本轮素材使用仓库既有应用图标，上传记录为 2094026058186186754、2094026657975853058；商品逻辑删除已核对，无前端恢复入口，可重建 QA 商品，未宣称存储文件物理删除。终端真实写回读不等于浏览器完整发布/刷新恢复验收；页面多步上传提交、真实网络故障、跨账号/跨刷新及其他创建仍待验收。未提交、推送或更新顶部承接。

- 求购创建恢复：重新读取 swagger-config、order 的 demands/create/my/page/detail/cancel、categories/tree/files/upload，以及 user 的本人地址列表/详情与递归 schema。求购限定启用的三级分类和最多 4 张参考图，期望天数为正 int32，7–30 仅为建议。API 增加原始本人记录/详情读取入口，保留 buyerId、addressId 和原始枚举用于核对，不使用展示适配器的默认值判断成功。
- 创建页已接入按账号持久保存的原请求、图片路径、旧 ID 集合及 unknown/confirmed/verified 回执；确认后重读分类、本人地址归属和地址内容，存储失败不发送，未知结果只接受唯一新增且全字段匹配的观察结果，不自动重发。隐藏/账号切换后的回执仍归原账号；核对后才能主动“发起另一笔求购”，只清本机记录并重置草稿。表单提交期间禁用编辑，选图实际处理数量封顶 4，访客/资料失败/无三级分类/回执损坏各自保留提示与恢复入口。
- 求购验证：10 组生产源码断言通过，覆盖原 Long ID/全字段与有序图片、51 条完整分页、旧/缺席/多匹配、存储失败与新实例、地址变化/删除、禁用祖先/三级分类、金额精度/int32、取消确认/互斥/迟到账号、隐藏成功及页面显式新草稿。商品发布 12 组、退款创建 11 组既有断言重新通过；类型检查、H5/App 构建和 diff 检查通过，无新增测试文件或依赖。
- 真实隔离证据：环境原无启用三级分类，通过部署后台新建 QA-H5-DEMAND-20260830 → QA-H5-DEMAND-L2 → QA-H5-DEMAND-L3（ID 2094031986071527426 / 2094032077012426753 / 2094032162416844802）。生产 API/恢复逻辑以既有应用图标上传 DEMAND 文件 2094032281455386625，创建求购 2094032284357844993，精确回读后取消；Chrome 填写 U 1/7 天、仅三级分类可选、取消确认后接口无新增，再创建 2094032551400792066，待审核详情与原字段回读通过，新页面实例恢复原回执、查看原结果、撤销后 CANCELED、显式新草稿重置均通过。后台历史页交叉回读两笔均已取消，无关联订单；未审核、抢单、付款或修改历史求购。QA 分类根节点已停用，可重新启用；取消记录和上传文件保留，未宣称物理清理。真实超时/存储故障/跨账号竞态、大分页和浏览器选图上传仍未验收，不把源码断言或终端上传当作这些分支通过。未提交、推送或更新顶部承接。

- 评价创建恢复与资格扫描：本批重新核对实时 review create/reviewable/mine/detail/delete 及递归 schema；创建按订单幂等、重复返回原评价 ID，删除不恢复评价资格。页面接入按账号持久的原请求/订单关联/unknown-confirmed-verified 回执，发送前重新核对本人 COMPLETED 订单和资格；未知仅允许只读核对或确认后按原内容重试，不允许改内容再发。回读逐项核对原 ID、用户/卖家/商品、双评分、文字、匿名标志及有序图片；迟到回执归原账号，存储损坏不抹掉已知结果。
- 待评价资格与未知评价扫描每轮最多 5 页、每页 50 条；成功页才推进，失败从原页继续，列表变化重置，未查完不判定不可评价。完整空扫描后再次核对重新读取，不沿用旧总数。评价列表独立提供未完成创建入口，已受理后即使不再待评价也能恢复；提交核对后及查看原记录直接进入“我发出的”。13 组源码断言与求购 10 组既有断言通过，覆盖 251 条续查、漂移/重复/失败、原请求重试、防重、存储故障、新实例及迟到账号。类型、H5/App 构建和 diff 检查通过；无新增测试文件或依赖。
- 评价真实验证：主测试账号无待评价，第二账号有 5 条；选用既有隔离 QA 订单 2088549990985719810（展示号 2088549990973136896），Chrome 新建评价 2094037533512523778，回读 PUBLISHED/双 5 分/原文字，待评价降至 4 条；重新进入及新标签页恢复同一原回执，查看记录直接展示本次评价；卖方测试账号 API 独立回读一致。Chrome 仅删除本轮新评价，列表与 API 确认缺席，订单仍 COMPLETED、不可重评；逻辑删除不恢复资格，历史其他评价未修改，未付款或改变订单状态。真实超时后的原内容重试、存储故障、跨账号竞态、大分页及浏览器图片上传仍未验收，不能以源码断言代替。KYC 创建与其余全局整改继续保留；本批未提交、推送或更新顶部承接。

### 全局复查补充清单（不替代原 16 组目标）

以下保留先前 14 组复查编号；后续复查新增缺口在表后保留，不替代原 16 组整改与真实验收范围。代码落地不等于真实验收完成。

最新只读复查的 13 组问题仍纳入上述完整目标：创建结果恢复、履约/撤回持久保护、KYC 切换证件类型后的影像失效、商品返回刷新/零库存数量、收藏互斥、售后/身份真实性、私有页面状态、求购约束、待评价有限扫描、商品/收藏分页、深链接与错误恢复、AI 演示及响应关联、非实时汇率标识。求购创建及约束、评价创建与有限扫描已落地，真实正常证据与未验收异常见上方，不能据此关闭其他分支；KYC 类型变化不得继续复用旧证件图，商品零库存“＋”不得把数量变成 0。

| 编号 | 问题 | 当前推进状态 |
|---|---|---|
| 1 | 登录失效回跳丢业务参数 | 已补逐页面参数及登录嵌套编码保护；真实失效回跳待验收 |
| 2 | 旧确认弹窗及重复确认 | 退款、评价、求购双入口及 IM 撤回已补代码；IM 本轮新消息取消/确认撤回及重载通过，跨账号/离页旧确认仍待真实验收 |
| 3 | 私有页面会话清理 | 钱包记录、理财概览、退款、评价、求购及 IM 清理/初始化重试已补代码；账号切换源码断言通过，真实跨账号与失败恢复待验收 |
| 4 | 付款提交与回读耦合 | 三个入口已拆分并共享持久回执；只读精确核对及本地异常断言通过，真实付款异常仍待验收 |
| 5 | 未知提交结果及跨入口防重 | 已有结算/资金/抢单及付款/退款创建与撤销/商品发布/评价管理/求购创建与撤销、订单取消/收货、充值取消和地址新增/默认/删除持久保护；求购本批完成隔离创建/详情/新页面实例恢复/取消及后台回读；商品浏览器完整发布恢复仍待验收；评价创建已补并完成正常创建/恢复/删除回读，KYC 创建已接入待专项/真实验证，发货/物流及 IM 撤回跨刷新仍待补齐，真实异常仍待验收 |
| 6 | 退款详情半更新及成功状态倒退 | 已补完整关联校验、原子更新与共享回执；源码验证通过，真实异常写分支待验收 |
| 7 | 理财假收益走势 | 固定走势及无效筛选已移除；真实非空页面验收待完成 |
| 8 | 未登录/资料失败误显示空记录 | 钱包记录、理财概览、退款、评价、求购、IM 列表、地址页及会话初始化已补；钱包首页仍待补，真实登录失效与资料失败仍待验收 |
| 9 | 钱包记录错误提示与重试 | 三页已补保留式刷新和原页重试；真实多页/失败场景待验收 |
| 10 | 评价回复/申诉草稿丢失 | 已补当前页面草稿预填、失败/取消保留及账号清理；源码断言通过，真实失败重试待验收 |
| 11 | 商品详情返回刷新及收藏互斥 | 待整改 |
| 12 | 深链接返回兜底 | 部分入口已有；商品、钱包等自定义返回仍待补齐 |
| 13 | AI 演示标识及并发响应关联 | 待整改；不把 Mock 迁移为未声明真实能力 |
| 14 | 公共分页离页/刷新归属 | 已补私有记录、退款、评价、求购、IM 隐藏隔离与原页重试；求购及待评价资格/未知评价每轮最多 5 页并可继续，评价 251 条源码续查通过；其余公开分页和真实大分页异常仍待收口 |


后续代码复查新增/细化：商品未知售后类型不得默认宣称“7天无理由”，卖家等级/认证不得固定宣称；待评价资格已补有限扫描和继续入口，真实大分页异常仍待验收；钱包首页仍需区分未登录、资料失败与真实空数据；人民币参考价仍取本地 Mock 汇率，须明确非实时参考性质，真实汇率接入须有有效契约；AI 匹配不足时补充的热销商品须与关键词匹配结果区分，不能统一宣称相关搜索结果。除已明确更新的评价扫描外，上述仍未整改，不因其他批次完成而移除。求购创建已按有效契约收敛为三级分类/最多 4 图，并接入持久恢复；源码与本批正常创建/新实例恢复/取消已验证，真实异常分支保留。地址管理页面/会话/弹层隔离、空态与持久回执及结算回传保护已补；退款创建原申请恢复与收货互斥、订单取消/收货原 ID 回执校验、充值取消持久防重及已取消结算精确收口已落地，仍保留地址菜单/真实异常、退款创建/撤销异常、充值取消与收货结算验收。

下一步：保留原 16 组整改、后续全局复查问题及真实验收范围，不替代或缩小原目标。退款、商品发布、付款、评价管理、求购创建/撤销、订单取消/收货、充值取消、地址管理隔离/创建恢复及 IM 会话隔离/初始化/撤回确认、结算严格存储/取消收口已补代码；评价创建已补并完成正常创建/恢复/删除回读；继续验证 KYC 创建恢复，并处理发货/物流和 IM 撤回跨刷新恢复，再处理商品返回/收藏/未知售后与身份展示、钱包首页空态、人民币参考汇率标识、深链接、AI 演示/响应关联与剩余分页/资格扫描。原有购物车账号隔离/游客交接/旧版恢复、金额变化确认、未知结果恢复、独立地址选择及地址菜单/异常、多页筛选、通知历史/计数/删除/清空、表单账号切换/上传/成功回执/驳回重提、保证金同键恢复与赎回回读、商品上下架/删除及真实权限、订单/充值/退款/求购/评价写链路、双账号实时及断线补偿继续保留真实验收要求。Chrome 已恢复输入并完成测试登录，可继续创建可控测试条件；本轮新待付款订单已取消，其本机结算进度已核实清理；本轮隔离地址已删除并恢复原默认；求购两笔 QA 已取消，独立分类根节点已停用，不对历史已付款订单或未知结算执行写操作。后端金额版本原子校验、部分创建接口幂等键与未知付款结果核实条件继续保留。不把代码落地、源码断言或此前主流程通过视为本轮目标全部完成。累计整改代码与任务文档已通过 73cf204 推送到 main，整体仍为部分完成；顶部承接状态未更新。

## 梯队进度

梯队按“契约确认、API/核心逻辑、页面接入、真实数据验证”综合评估。接口存在、仅空态，或未覆盖写操作时，均不视为完整验收。下表是 2026-08-29 主功能历史基线，尚未纳入本轮新增整改分母，不代表当前完整目标完成率；不得简单平均或继续沿用为本轮验收比例。

| 梯队 | 当前能力 | 完成度（估算） | 回归度（估算） | 已完成 | 剩余边界 |
|---|---|---:|---:|---|---|
| P0 联调地基 | 请求、登录与基础资料 | 100% | 100% | `uni.request` 请求层、登录态、Long ID、分类树、地址、当前用户 | 无前端基础设施待办；后续模块仍须实时复核 Swagger |
| P1 账户与资金读取 | 积分、钱包、买手/KYC 状态 | 92% | 88% | 积分账户/流水/申诉记录、积分规则、VIP 全等级配置、钱包总览/流水/充提记录、买手申请状态、KYC 状态与文件上传/提交 | 新隔离 QA 账号已完成 KYC 三张合成影像上传、签名地址换取和 `PENDING` 提交；签名 URL 指向不可达的 `127.0.0.1:8000`，实际私有影像 GET/页面展示待后端修正存储地址 |
| P2 商品、求购与买手 | 公开商品、收藏、求购、买手经营 | 90% | 84% | 商品列表/详情/排序、收藏、商品删除、求购创建/取消/抢单、求购参考图上传、买手申请、商品创建/上下架、买手仪表盘真实读取 | 求购创建/撤销已通过 H5 隔离数据回归；推送轨迹/日志及部分经营字段仍缺契约，抢单需合适 KYC 买手账号补写回归 |
| P3 交易闭环 | 购物车、结算、订单、仅退款 | 96% | 93% | 地址选择、真实商品隔离、合并下单、订单组付款、买卖订单、carrier 发货、采购单号/履约凭证上传、物流详情/轨迹/异常展示、签收、仅退款创建/撤销 | 单品 `6.00 U` 已完成支付、发货、物流非空与签收回归；2026-08-29 隔离商品已完成审核上架、跨账号订单组支付、采购/发货凭证上传、运输轨迹和签收回读；另一隔离订单已完成仅退款申请、详情与撤销后订单回到 `PAID`；付款失败幂等重试及退款审核分支仍未验收或缺契约 |
| P4 资金与会员 | 充值、提现、买手保证金、理财、评价 | 82% | 70% | 资金读取、充值取消、买手保证金流水/缴纳/退还、理财产品/申购/锁仓回读、评价提交与列表回读；评价回复、买手申诉、评价删除已用隔离评价回归 | 充值申报、取消、提现创建、保证金缴纳/退还、理财提前赎回仍待受控真实写回归 |
| P5 通知与专题 | 通知、IM、KYC、CMS、AI | 90% | 86% | 通知 REST、会话/历史消息、已读/撤回、图片/语音入口、认证状态降级、KYC 上传/提交、IM 子协议适配；H5 SocketTask、双账号无刷新到达、重连与历史补偿；合成 M4A 语音上传/发送/资源回读与 H5 播放入口检查 | KYC 私有影像实际资源 GET 受存储地址配置阻塞；CMS 与 AI 仍无真实接口 |

## 已完成与验收结论

### 基础、账户与商品

- 已建立 `src/service/request/` 与按领域拆分的 `src/service/api/`；统一处理 baseURL、`X-Access-Token`、成功码、业务错误、登录失效和响应解包。
- 登录、当前用户、分类树、地址、积分账户/流水/申诉记录、钱包总览/流水、充值/提现记录与详情均已页面接入真实接口。
- 公开商品、详情、收藏、买手商品、求购和买手申请已接入；真实商品与 Mock 商品以 `source` 隔离，不能混入真实结算。
- 买手仪表盘已读取真实卖出订单、买手商品、求购大厅和保证金流水；缺失的评价率、客诉率、发货时长等经营统计不再以 Mock 数据展示。
- 业务 Long ID 在真实类型、列表键、路由、选择状态与写操作中保留原值，不在页面层做数值转换。

### 交易与资金

- 已完成真实商品的“加购 → 真实地址/余额 → 合并下单 → 订单组付款 → 买手发货 → 顾客签收”回归；2026-08-26 另完成单品 `3.00 U + 1.00 U 运费 + 2.00 U 税费` 的 `6.00 U` 下单、支付与已付款回读；创建成功后的付款重试复用同一订单组，避免重复建单。
- 商品详情“立即购买”已与购物车结算隔离：使用带 `contextId` 的独立持久化上下文，只结算当前商品，登录回跳、页面刷新和地址页返回均保留本次购买意图；成功后只清本次上下文，不改动购物车商品、数量或勾选状态。2026-08-29 本地 Chrome 移动视口以购物车已有“测试数据3 ×2”为基线，点击“立即购买”并取消登录后数量与总额保持不变，登录地址保留 `mode/contextId`，无控制台错误；本轮未重复执行真实付款。
- 买入/卖出订单、详情、取消、确认收货、买手发货与仅退款（创建、双方分页、详情、顾客撤销）均已接入真实接口；后端 7 状态仅在 adapter 映射为现有展示标签。
- 钱包总览、资产桶、资金流水、充值/提现记录和详情已真实读取；买手保证金已切换为真实流水、缴纳和退还接口，理财产品、申购和锁仓回读已完成。测试环境充值或开发回调不能表述为真实链上到账。

### 通知、评价与 KYC

- 评价已完成真实提交和列表回读；已评价订单不会重复展示“写评价”。
- 通知已接入分页、未读数、单条/全部已读、删除和清空；IM REST 已接入会话、历史消息、已读、撤回、文字/图片/语音发送、刷新回读和失败重试。已验证非空读取、单条已读和订单群文字发送后刷新回读。
- KYC 已改为真实状态读取、私有证件影像上传、`fileId` 提交和按需签名地址刷新；新隔离 QA 账号已用合成非真实证件完成三张影像上传、签名地址换取和 `PENDING` 提交回读。签名 URL 当前指向不可达的 `127.0.0.1:8000`，实际私有影像 GET/页面图片展示仍待后端存储地址配置修正。

## 当前有效验证证据

| 能力 | 当前有效证据 | 结论 |
|---|---|---|
| 地址管理 | 真实读取、新增、设默认、删除后回读 | 通过 |
| 主交易 | 真实商品下单、订单组付款、买手发货、顾客签收；单品 `6.00 U` 下单/支付/发货/物流非空/签收回读 | 通过 |
| 订单取消与仅退款 | H5 取消专用待付款单；仅退款创建、列表/详情、撤销和订单状态回读 | 通过 |
| 商品与收藏 | 非空商品读取、排序、详情；收藏、列表回读和取消 | 通过 |
| 理财与评价 | 理财申购后锁仓/通知回读；真实评价提交/列表回读；隔离评价买手回复、申诉状态和删除回读 | 通过 |
| 买手保证金 | 真实保证金流水分页返回成功码 `1`；页面已移除 Mock 与“模拟成功”路径 | 读取通过，缴纳/退还待受控写回归 |
| 买手仪表盘 | iPhone 15 Pro 尺寸登录态读取卖出订单 `10`、在售商品 `6`、可接求购 `0`、保证金 `0.00`；控制台无错误 | API/页面/非空移动端读取通过 |
| IM REST | 订单群历史消息、文字发送、刷新回读 | 通过 |
| 通知 REST | 非空通知、单条已读与未读数回读；单条删除、清空后的空态回读 | 通过 |
| KYC 与 IM 媒体 | 合成 QA 影像上传/签名地址/`PENDING` 提交；合成 1 秒 M4A 上传、发送、公开资源回读及 H5 播放入口 | API 与媒体资源上传通过；KYC 私有资源 GET 受 `127.0.0.1:8000` 地址配置阻塞 |
| IM 实时 | 2026-08-26 服务端直连及本地 Vite 代理均对测试账号返回 `READY`；H5 已适配子协议、二进制消息解码和 SocketTask 回调获取，双账号无刷新消息到达、重载后重连与历史补偿均已验证；合成 M4A 语音已完成上传、发送、公开资源回读和 H5 播放入口检查 | 通过；断线增量补偿仍待运行时验证 |

最近适用检查：

- `pnpm typecheck`：通过（2026-08-22）。
- `pnpm build:h5`：通过（2026-08-22）。
- 本批相关文件 `git diff --check`：通过（2026-08-22；`pages.json` 的用户本地修改未纳入检查）。
- `pnpm build:app-plus`：通过（2026-08-22）；静态扫描未发现真实业务页面直接依赖浏览器专属运行时。
- iPhone 15 Pro 尺寸 H5：登录后“我的”、买手仪表盘与保证金流水均完成真实读取；Android/iOS 真机验收尚未完成。

## 未完成与后端补充清单

| 优先级 | 后端/环境需补充 | 前端恢复点 |
|---|---|---|
| P1 | KYC 私有资源的可达存储地址 | KYC 上传、签名地址换取和提交已完成；待修正 `127.0.0.1:8000` 存储地址后回归实际影像 GET/页面展示 |
| P1 | 安全测试数据与可操作后台流程 | 验证买手申请驳回后重提及其他尚未覆盖的后台状态分支；评价回复/申诉/删除、通知删除/清空已完成 |
| P1 | 充值/提现、提前赎回的可控测试条件与业务响应 | 完成写入、失败提示、资金/状态回读；不得伪造到账或资金结果 |
| P2 | 订单履约凭证、完整售后及费用字段 | 补订单详情的非空展示与完整售后交互；物流轨迹/异常入口已接入待写回归 |
| P2 | 商品删除、求购手动推送/轨迹/日志及买手经营关键字段 | 替换对应 Mock 或补齐运营展示 |
| P5 | CMS 公告、帮助、协议及 AI 导购的 C 端契约 | 替换当前 CMS/AI Mock，完成真实数据回归 |

## Mock 与交付边界

- 已接入真实接口的模块请求失败时不得回退 Mock，也不得伪造成功状态。
- 未迁移模块可以维持现有 `@shared` Mock；CMS、AI 和演示数据只能计为页面交互，不能计入真实接口验收。
- H5 开发与部署使用同源 `/api/*` 代理；Android/iOS 仅使用 HTTPS 服务地址，不依赖 H5 代理或浏览器能力。
- H5 为当前先行验收端；Android/iOS 在模块里程碑集中验证安全区、软键盘、返回、滚动、固定操作栏和错误提示。

## 接口接入原则

1. 修改前刷新对应 live Swagger，递归核对路径、方法、参数、必填项、requestBody、response 与嵌套 schema。
2. 页面交互保持现状；路径、字段、类型、枚举、分页与响应结构以当前有效契约为准。
3. 字段差异只在 `src/service/api/` 或 adapter 边界转换，不将后端 DTO 扩散到页面。
4. 所有业务 ID 按不透明 Long 原值处理，页面层禁止 `Number()`、`parseInt()` 或算术转换。
5. 分开记录“Swagger 存在、API 已封装、页面已调用、真实数据已验证”，任一层不能替代另一层。
6. 真实接口失败必须展示真实失败、权限或空态，不自动 fallback Mock。
7. TypeScript/公共逻辑变化运行 `pnpm typecheck`；构建、环境或跨端基础能力变化运行对应构建；页面交互使用 Chrome 移动视图回归。

2026-08-22 补充：通知与保证金读取失败不再清空既有真实数据或误显示“暂无”；买手工作台订单区明确为“最近卖出订单”。真实订单和真实 IM 路由已静态核对不进入旧 Mock 订单群页面；旧 Mock 页面仅保留给其演示数据入口。Chrome iPhone 15 Pro 尺寸已登录回归确认买手工作台、保证金空态、通知非空列表和钱包读取正常；消息中心离开时的 SocketTask 清理异常已修复并复测无新增控制台错误。

2026-08-22 写回归预检：测试账号买手申请为 `APPROVED`、保证金流水为 `0`、理财订单均为 `REDEEMED`、求购大厅无可抢任务；充值/提现仅有既有终态记录。可评价订单、收到评价和非空通知存在，但未标识为可回滚的专用测试数据，因此未执行创建评价、评价治理、通知删除/清空或任何资金写入。求购真实 DTO 的 Long ID 类型已改为原值透传，不再伪装为 Mock 的 `number`。

2026-08-22 前端收口：订单群、消息中心、求购大厅、充值链和评价页的读取失败均不再伪装为空态；Chrome iPhone 15 Pro 尺寸复核求购大厅真实空态、订单群入口和待评价真实记录，未见新增控制台错误。

2026-08-22 消息中心补充：系统通知与交易通知改为按真实 `bizType/templateCode` 分类展示各自的最新内容和未读数；当 IM 网关无在线连接时明确展示实时不可用，不再仅因存在网关路径而标记为已就绪。

2026-08-22 阻塞复检：实时 Swagger 仍为 `admin 159/160/275`、`user 41/41/80`、`order 57/59/85`、`notify 17/17/24`（路径/操作/schema），未发现新契约；测试账号登录成功，但 Notify 状态仍为 `onlineConnections: 0`，`wss://…/api/notify/im` 握手仍失败，P0 实时联调继续等待网关恢复。

2026-08-29 全功能回归收口：部署测试环境的两组测试账号均完成账户、积分、钱包、充值/提现记录、买卖订单、售后、求购、理财、通知、IM 会话/历史/增量和评价只读矩阵，相关接口均返回成功码 `1`；首页及商品公开接口继续返回真实数据，闪购/横幅为空时页面正确展示空态。Chrome 移动视口逐页复核 50 条路由及订单/理财/售后未登录列表修复，均无空白页、控制台 error/warn 或未处理异常；结算页仅按预期重定向登录。当前仅保留 KYC 历史文件、充值链地址、IM 断线运行时、语音安全素材和 CMS/AI 契约等外部条件阻塞，已跳过并继续完成其余可执行项。

2026-08-29 前端独立回归补充：为求购大厅/创建/详情、收藏、消息中心/通知、IM、理财、钱包记录、订单、售后、评价、结算成功和买手商品详情补齐未登录请求守卫，20 条受保护直达路由复核均无 Token 错误、控制台异常或未处理 Promise；收藏页改为 `onShow` 刷新并完成“收藏→我的收藏回读→取消收藏”页面闭环。john 账号通过 H5 创建隔离求购 `QA H5 newUI 前端创建求购 20260829-0031`，详情回读为“待审核”后页面撤销并回读“已取消”；同账号创建隔离五星评价，mamba 账号在“我收到的”页面回读并完成买手回复，买手申诉接口与页面状态回读为 `PENDING`。顾客侧误发申诉返回真实业务失败，未伪装为成功。以上写入均为测试环境隔离数据，凭据未写入文档或提交。

2026-08-29 前端独立写回归补充：在隔离评价上完成买手侧删除并刷新回读，评价治理链路（提交、回复、申诉、删除）均已通过；在通知页完成单条删除和“清空”确认，页面回读真实空态。订单群新建文字消息后立即撤回，发送端显示“消息已撤回”，重新进入页面仍保持撤回态；此前超过服务端撤回时限的消息按真实失败提示处理，未伪造成功。以上数据均为测试环境隔离写入/删除，当前同源 Chrome 标签共享 storage，不将本轮标签复核作为独立双账号证据。

2026-08-29 KYC/语音素材回归补充：新建隔离 QA 账号，生成带“QA TEST / NOT REAL”标记的三张 PNG 影像，经 `/kyc/files/upload` 全部成功，`/kyc/files/access` 均返回私有签名地址（300 秒），`/kyc/submit` 成功并回读 `PENDING`，未使用真实证件信息。合成 1 秒 M4A 经 `/im/files/upload?scene=IM_VOICE` 成功，`/im/messages/send` 成功，另一会话分页读回 `VOICE`、时长 1 秒和公开音频 URL；H5 订单群语音入口点击无控制台错误。KYC 签名 URL 的资源域名仍为不可达的 `127.0.0.1:8000`，因此影像实际 GET/页面图片展示暂记后端配置阻塞；临时素材位于 `/tmp`，未进入仓库。

2026-08-29 持续回归补充：本地 H5 完成订单“全部/待付款/待发货/待收货/已完成”、求购“全部/待审核/推送中/已接单/已驳回/已取消”、售后“全部/待审核/已同意/已驳回/已撤销”、理财“持仓中/已结算/已赎回/已取消”和买手商品“全部/待审核/在售/已下架/已驳回”页签切换，均无控制台 error/warn；买手商品详情上下架确认弹窗可打开并取消，保证金缴纳/退还底部弹层可打开并关闭；充值页在 ETH/TRON/BSC 地址不可用时展示后端真实 `Empty key`，未伪造地址或申报成功。以上均为读取或取消操作，未执行资金写入、商品状态写入或不可逆确认。

2026-08-29 页面失败态收口：公开/买手商品详情、求购详情、仅退款创建与详情、评价提交、理财详情、充值/提现详情、地址管理、钱包总览/最近交易和订单群均已区分“加载中、接口失败、真实不存在或空数据”，请求失败不再伪装为无数据，也不覆盖已加载的真实内容。Chrome 移动视口已验证真实商品、真实退款、真实地址、钱包资产与流水、从消息列表进入的真实订单群正常展示；无效商品、求购、理财、充值和订单群请求均展示明确失败态，全程无控制台 error/warn。`pnpm typecheck`、`pnpm build:h5`、`pnpm build:app-plus` 和 `git diff --check` 均通过。

2026-08-29 并行模块失败态补充：首页在任一分类/商品/活动接口失败时展示“部分内容加载失败”，买手工作台分别保留卖出订单、在售商品、求购和保证金的成功结果，失败指标显示 `—` 且列表不再误显示真实空态；订单详情区分主体加载、主体失败和物流子请求失败，商品详情对评价子请求部分失败给出提示。Chrome 移动视口已验证首页推荐/热销/新品、买手工作台真实指标与卖出订单、从订单列表进入的真实订单详情及非空物流轨迹，均无控制台 error/warn。订单列表展示订单号而路由使用订单实体 ID，此前把展示订单号手工作为详情 `id` 的失败结论已撤销。

2026-08-29 最终静态审计补充：买手商品和积分页已将用户初始化纳入异常处理；买手商品的分类树读取失败不再阻断商品分页；充值链地址请求增加切换令牌，快速切换 ETH/TRON/BSC 时旧响应不能覆盖当前链，创建申报的登录初始化异常也会正常收口。Chrome 移动视口验证买手商品与积分真实非空数据、三链快速切换和后端 `Empty key` 失败提示，无控制台 error/warn；业务 Long ID 静态复核未发现真实接口页面使用 `Number()`、`parseInt()` 或 `parseFloat()` 转换。

## 跨设备承接

1. 先执行 `git status -sb`。工作区干净时运行 `git fetch origin` 与 `git pull --ff-only`；有本地修改时先检查，绝不覆盖或重置。
2. 阅读本文、[AGENTS.md](../AGENTS.md)、[README.md](../README.md) 和 [Swagger 匹配矩阵](./api-swagger-match-matrix.md)，再结合当前代码与 live Swagger 确认状态。
3. 仅在依赖缺失或锁文件变化时运行 `pnpm install`；优先复用已有服务和 Chrome 登录态，测试凭据不输出、不提交。
4. 恢复工作优先修正 KYC 私有资源地址并复核影像展示，再按实时 Swagger 和对应恢复条件推进物流轨迹/异常、充值/提现、理财赎回及断线补偿。

历史测试 ID、逐次回归过程、旧 Swagger 数量和旧进度百分比需要追溯时，使用 `git log -- docs/api-integration-plan.md` 与 `git show <commit>`。
