# C-H5 真实接口接入与承接计划

> 本文只维护当前有效状态、已验收能力、剩余缺口和下一步。接口路径、字段和页面调用详情见 [Swagger 匹配矩阵](./api-swagger-match-matrix.md)，开发规则见 [AGENTS.md](../AGENTS.md)。历史开发过程、旧 Swagger 快照和测试过程以 Git 记录为准，不在本文重复追加。

## 当前承接状态（2026-08-29）

| 口径 | 当前状态 |
|---|---|
| 分支 | `main`；准确提交位置以 Git `HEAD` 为准 |
| 结论 | **部分完成**；H5 主交易、账户、资金读取、隔离履约、仅退款、求购审核、IM 图片/语音和 KYC 提交已完成真实回归，剩余工作集中在 KYC 私有资源地址、断线运行时、资金受控写入与缺失契约 |
| 实施进度 | 约 `94%–96%`；仅作当前能力分层估计，不能替代逐项验收 |
| 真实回归度 | 约 `86%–90%`；按真实请求、页面展示、响应及刷新/详情回读统计，后端阻塞项仍计入未回归，真机验收按当前任务排除 |
| 主链路 | 真实地址、商品、购物车、合并下单、订单组付款、买手发货、顾客签收和仅退款已完成真实回归 |
| 本批完成 | C-PC 页签图标同步；KYC 文件上传、`fileId` 提交和签名地址刷新；carrier 发货、采购单号、采购/发货凭证上传、物流详情、卖家轨迹/异常入口；求购审核状态/地址/驳回展示与参考图上传；商品详情卖家/分类真实字段；订单群兼容 `IM_MESSAGE`、`IM_READ`、`IM_RECALL`，已改用真实 `realUserId` 判断本人消息，以 Long 安全、单调阅读水位线展示他人已读状态；断线恢复后按服务端 `GET /im/messages/incr` 增量补偿，补偿失败时保留待补偿标记以便后续重连重试；消息首页与通知详情在实时事件到达后刷新未读数和摘要，消息首页以本机 Socket 状态展示连接结果而非全局在线数；图片/语音收发播放与实时状态更新；WebSocket 按 `apiPrefixPath/gatewayPath` 与 `tokenSources` 自适应连接并遵守服务端心跳上限；通知精确跳转；积分规则/VIP 配置字段补齐；商品删除与充值取消；新版移动端视觉替换已覆盖页面结构、图标与安全区，已恢复首页业务模块、商品详情轮播、分类树层级与双栏独立滚动、购物车操作，并统一原生顶部导航白色背景；首页商品区与“我的”资产区已移除未经真机验收的 CSS Grid，改为等价 flex 换行布局；2026-08-29 已完成两件隔离商品审核上架：第一件完成跨账号支付、凭证发货、物流轨迹、买家回读与确认收货闭环；第二件完成支付、仅退款申请、详情回读和撤销；隔离订单群的 IM 图片已完成上传、发送、卖家回读和撤回态回读；隔离求购已完成参考图上传、创建、后台驳回及 C 端审核意见回读；订单、理财、售后列表未登录直接访问触发真实请求的问题已修复为先初始化用户并在无会话时安全退出；50 条页面路由访客态全量复核无空白页、无控制台 error/warn 或未处理异常；新增受保护详情/列表页登录守卫覆盖求购、评价、订单、售后、理财、钱包、IM、买手商品与收藏，未登录直达不再发出真实请求；收藏页改为 `onShow` 刷新，完成收藏→列表回读→取消收藏闭环；以测试账号通过 H5 页面创建并撤销隔离求购，顾客提交评价、买手回复及买手申诉均完成接口与页面回读 |
| 本轮追加 | 评价删除、通知单条删除与通知清空已在测试环境完成页面确认和空态回读；订单群新建文字消息后立即撤回，刷新页面仍显示“消息已撤回”；Chrome 本地 H5 完成购物车删除确认取消、地址新增底部弹层关闭、求购分类 ActionSheet 选择、商品费用说明弹窗、帮助文章与协议底部弹层的打开/关闭回归。发现帮助中心 `wd-collapse` 未绑定 `v-model` 导致分类无法展开，已补充受控展开状态并验证文章弹层可打开关闭；求购“全部/待审核/推送中/已接单/已驳回/已取消”和售后“全部/待审核/已同意/已驳回/已撤销”页签均完成本地 H5 切换，无控制台 error/warn；订单群真实会话页面显示实时连接可用，页面重载后历史消息、图片/语音入口和消息撤回态保持，控制台无 error/warn。当前 Chrome 能力不提供网络离线/强制关闭 Socket 的控制入口，因此断线增量补偿仅保留代码与接口证据，不冒充运行时通过。本轮两个同源 Chrome 标签共用 storage，不将该标签复核作为独立双账号证据；既有独立双账号实时证据继续沿用 |
| 前端稳定性排查 | 买手保证金、订单/物流、消息通知和评价时间字段已兼容数字时间戳、数字字符串与标准日期字符串，保证金流水不再显示 `Invalid Date` 且排序保持正确；订单、仅退款、求购、评价、理财持仓页签增加请求序列门禁，快速切换时只接纳最后一次响应；订单、售后、评价、通知、结算和理财的读取/写入失败补齐可见错误态与失败提示，不再将接口失败伪装成空数据或产生未处理 Promise。旧 Mock 订单群路由仅由未使用的演示组件引用，真实入口仍全部进入真实订单群，按既有迁移边界保留未删除 |
| 本轮稳定性回归 | Chrome iPhone 15 Pro 尺寸复用登录态完成保证金流水、订单、仅退款、求购、评价、理财产品/持仓、结算、消息与通知读取；保证金两条数字字符串时间显示为有效本地时间，订单/售后/求购/评价页签连续快速切换后最终数据与所选页签一致，结算页真实地址、商品快照、余额与金额正常，全部页面无新增控制台 error。`pnpm typecheck`、`pnpm build:h5`、`pnpm build:app-plus` 与 `git diff --check` 均通过 |
| 列表错误态补充 | 商品综合列表、买手商品、分类树/分类商品、收藏、积分、钱包流水及充值/提现记录已区分加载中、真实空数据和首次加载失败；分页追加失败时保留既有真实数据。分类首屏新增树加载态，避免接口尚未返回时短暂误显示“该分类暂无商品”。Chrome iPhone 15 Pro 尺寸已回归分类非空商品、公开商品非空列表、收藏空态、积分非空流水和钱包非空流水，均无控制台 error |
| 本轮素材验证 | 新建隔离 QA 账号后，以合成、明确标注非真实证件的 PNG 完成 KYC 三张影像上传、签名地址换取和 `PENDING` 提交回读；合成 1 秒 M4A 完成 IM_VOICE 上传、发送、公开音频资源回读及 H5 语音入口播放检查。KYC 签名地址当前指向不可达的 `127.0.0.1:8000`，资源 GET/页面图片展示保留为后端地址配置阻塞 |
| 最近检查 | 2026-08-29 已通过 `pnpm typecheck`、`pnpm build:h5`、`pnpm build:app-plus` 和 `git diff --check`；newUI 变更范围未发现浏览器专属运行时或未经真机验证的 CSS Grid。公开测试环境的分类、推荐、热销、新品和商品分页均实测返回成功且非空，横幅与秒杀为真实空数组；Chrome iPhone 15 Pro 尺寸已确认登录、首页、分类、商品列表与“我的”访客态正常渲染：首页推荐商品双列 flex 布局和原生 tabBar 正常，页面均无控制台告警；商品列表关键词搜索返回真实匹配数据，不匹配时正确显示空态；真实商品详情的价格、运费税费、库存、卖家和评价读取正常；购物车访客空态正常，结算页按预期重定向登录且未发生写入；50 条页面路由逐页新标签复核均无空白页、控制台 error/warn 或未处理异常，仅结算页按预期重定向登录。终端测试账号的账户、积分、钱包、资金流水、买卖订单、订单详情、通知、IM 会话、历史消息和 `GET /im/messages/incr` 均成功读取，订单详情首条请求曾出现一次临时 `code=-1`，换用另一真实订单后正常返回。Chrome 自动化对商品列表“最新”筛选的点击在输入派发阶段超时两次，未产生页面控制台错误，记录为自动化限制；此前 2026-08-27 已复核补偿失败不清除待补偿标记的实现并再次通过 `pnpm typecheck` 与 `git diff --check`；Notify Swagger 与状态接口可读，`/api/notify/im` 已实测返回 `101` 并回显 `im` 子协议；H5 测试账号登录后消息中心真实数据读取、页面重载和服务端在线连接回读均通过（`onlineConnections: 1`）；订单群历史消息、撤回渲染、已读上报和实时 ready 无前端错误；双账号 QA 文本已验证无刷新 `IM_MESSAGE` 到达、`IM_READ` 发送方显示“已读 1”，并立即撤回后双端实时替换为“消息已撤回”，断线增量补偿仍待运行时验证；本轮新增 H5 登录态逐页读取我的、钱包、订单、评价、通知、IM、求购、KYC 等页面，john/mamba 均无控制台 error/warn；收藏页刷新 bug 已以收藏→列表→取消回读验证；求购创建/撤销、评价提交/回复/申诉/删除、通知删除/清空和订单群文字发送/即时撤回刷新回读均通过（当前两个同源 Chrome 标签共用 storage，不将本轮标签复核作为独立双账号证据）；新增本地 H5 弹窗回归覆盖购物车删除取消、地址弹层、求购分类选择、费用说明、帮助文章和协议，帮助中心折叠 v-model 缺失已修复并通过同一组检查 |
| 当前阻塞 | KYC 历史详情因既有后端文件已删除不可读取，页面已降级保留“已通过”状态；新隔离 QA 账号的 KYC 上传、签名地址接口和 `PENDING` 提交已成功，但签名 URL 指向不可达的 `127.0.0.1:8000`，实际资源 GET/页面影像展示仍受后端存储地址配置影响；IM 图片/合成语音已完成真实回归；测试账号的 ETH、TRON、BSC 专属充值地址均返回后端 `Empty key`，无法创建 `PENDING` 充值单以验证取消；订单群断线增量补偿尚待运行时回归，当前 Chrome 能力无法注入离线或强制关闭 Socket；CMS、AI 仍无可替换的 C 端契约。帮助中心折叠交互已由前端修复并通过本地回归，不再计为阻塞 |
| 当前服务异常 | 无新增前端可复现的详情服务异常。已确认订单列表卡片显示的是订单号，点击详情时透传订单实体 ID；此前把显示订单号手工当作 `id` 请求详情导致失败，不属于后端详情服务异常。从真实列表点击进入后，订单详情、金额、物流摘要、凭证与轨迹均成功读取 |
| 下一步 | 前端可独立推进的公开/登录态页面、隔离求购/评价/通知/IM 写回归及本轮 KYC/语音素材验证已收口；后续仅在条件出现时继续验证 KYC 存储地址、订单群断线增量补偿、充值链地址及后台状态分支。真机验收按当前任务明确跳过，CMS/AI 仍等待有效 C 端契约 |

## 当前实施与交付范围

状态：**部分完成**。本节和匹配矩阵为当前实施/验收口径；上方跨设备承接按规则原样保留，其 8 月估算比例和旧阻塞不代表本期完成率。前批接口适配、回执保护与展示整改已交付，积分/钱包修复已于0aaed08交付main；本次第二批仅交付KYC原重申请入口修复与两份文档。不改业务架构、依赖、AGENTS、README或顶部承接，不把提交推送当部署/完整验收。

- 订单组付款已适配 confirmedAmount、逐单结果及 pay-result；只对剩余 CREATED 订单重新确认，旧付款结果与原请求留存。退款/充值/提现/申购已接同键回查，明确 data:null 才可确认后按原参数重试，缺数据/错单/已有成功却查空不放行。
- KYC 动态 schema 驱动证件、国籍、影像及重提；确认后刷新规则，变化保留草稿但停止本次发送。承运商来自启用字典，不回退硬编码；评价消费权威资格；求购采用服务端预算/天数/状态筛选，读取本人进度和真实买手统计。
- IM 历史分页显式 markRead:false，撤回清空内容/媒体/时长并停止播放，底部可见才推进已读。流水/积分筛选透传 false/0，时间按本机当天闭区间，内部划转不冒充外部支出。
- 已收口正常页面的研发状态说明：首页/我的去无效 AI 入口，旧 AI/公告/演示群/商品路由安全返回；帮助保留五类协议正文、版本和必要演示声明。仪表盘 Hero/KPI 优先；筛选按需展开；分类提示贴字段且保留禁用；钱包账户/类型中文及金额换行。后端原有 QA 内容不删除或隐藏。

## 累计安全与交互整改

保留原 16 组目标和后续复查范围，以下按执行域合并，不重复逐次日期流水。代码/内存断言通过不替代真实异常验收。

| 执行域 | 已实施的约束 | 仍需验证 |
|---|---|---|
| 交易、订单与角色 | Long ID/订单号区分，数量与精确金额校验；顾客/卖家按真实归属；确认起互斥，提交与回读分离，未知/旧状态不重新开放动作 | 付款部分成功/-312/丢响应、跨入口恢复、订单取消/收货/退款冲突及服务端并发保障 |
| 购物车与立即购买 | 账号/游客隔离、选中项 guestTransferId 单次交接，独立 contextId 只结算当前商品；旧无归属数据显式恢复且不勾选；存储失败回滚，失效上下文不回退购物车 | 真实跨账号/游客交接/旧数据恢复、金额变化、未知结算与清理失败 |
| 创建与管理回执 | 商品/求购/评价/KYC/退款持久原请求、ID及已知结果；旧回执与新申请隔离；地址/默认/删除、充值取消、保证金、赎回、订单变更和物流/撤回保留必要防重 | 超时、存储故障、迟到回执、跨刷新/跨入口真实异常；本机保护不等于跨设备原子性 |
| 表单/上传与页面归属 | 原账号/页面/弹层代次，原生相册返回允许原会话继续；切类型清影像、隐藏成功不重发/不强跳，旧 finally 不解锁新操作 | 实际媒体上传竞态、身份/规则变化、KYC 驳回重提、地址菜单及各申请审核 |
| 登录与私有数据 | 旧会话401不清新账号，网络失败保留凭据；登录回跳仅保留安全业务参数；积分失败不阻断基础登录；未知资产/等级不补零 | 正常顺序账号切换已有证据，异常网络/并行会话/在途切换未全验 |
| 分页/筛选/详情 | 成功才推进页码，失败重试原页；搜索草稿与生效查询分离，去重/总数校验；离页与旧筛选拒绝迟到响应；详情返回重读、子请求独立失败 | 深分页、故障恢复、快速切换及最新折叠筛选的剩余点击；无样本不造数 |
| 消息与通知 | 原 clientMsgId/mediaFileId 重试，不重复上传；500条增量分批续接，重复READY不叠心跳；已读串行且可见底部；通知更新不重置历史，写/计数响应隔离 | 双账号新媒体/可见已读/撤回、断线补偿/500条失败续接、历史撤回治理 |
| 真实展示与权限 | 未声明状态/身份/售后不猜测；未知活动时间不宣称进行中，VIP预览不切身份；人民币折算仍标参考，缺接口不造模块/成功 | 真实活动到期、未知状态及完整售后范围；正式汇率与协议另待契约/产品决定 |

## 最近有效验证

### 本期接口适配与只读回归

- 第二批基线0aaed08：实时定向核对order的8条履约/分类消费者相关路径及16个递归schema、user的5条KYC路径及10个递归schema；本次未改API/类型，KYC已从旧v1更新为当前v6。发货、凭证上传、物流、KYC及本人求购进度的原提交/回读/会话隔离继续保留，不改共享配置、不造样本。此处接口读取不等于写链路验收。
- 2026-09-05 user 曾 404，恢复后本人资料及契约已可读，不再作为当前阻塞。组付款/同键回查/字典/KYC schema/评价资格/统计/求购进度/筛选/IM显式已读已接 API 和页面；具体字段和真实验证分层见矩阵。
- 9 月已有真实 API 证据：三类资金 by-key 返回明确 null；承运商16条启用；本人求购进度可读；经营统计数字字符串在 adapter 修复后显示12.5%、33.33%、0.87小时。评价9个本人订单资格覆盖已评价/未完成；新文本同 clientMsgId 返回同消息，markRead:false 不消耗未读，新撤回双方历史/增量内容和媒体清空。真实资金成功/-311、KYC新配置闭环、新媒体/Socket等未全验。
- 前批 Chrome iPhone 15 Pro 筛选验证：钱包业务号/账户桶/详情返回，积分方向/多行为/日期双边/逆序错误及原重置，求购取消+预算1+7天命中2条、详情返回保留、重置6条。原QA→第二QA→原QA 顺序切换后三页快照恢复一致：钱包均39条但关键记录不同、积分均100但时间不同、求购6→4→6。仅正常顺序切换，不等于异常竞态全覆盖；50条页大小无第二页、真实扣分缺样本。
- 前批公开商品导航曾触发既有 POST /storefront/browse 浏览记录/计数写入，不能宣称那批零写；后续未重复、未删除补偿。最新展示批次不进入商品详情/真实会话/未读通知。
- 本期历史内存验证包含付款、退款、资金恢复、KYC规则及评价401 ID分批等记录；不是仓库可重放测试套件，也不是实际资金/审核/媒体验收。本次提交检查按下方新结果记录，不能把历史断言说成本轮重跑。

### 原主流程证据与关键复现线索

| 流程 | 保留的历史证据 | 边界/残留 |
|---|---|---|
| 主交易 | 8月真实地址→加购/独立购买→合并下单→组付款→发货→签收；8月26日含运税6.00 U；8月29日隔离凭证发货/轨迹及顾客回读 | 新 confirmedAmount/部分结果契约不能继承为写验收；详情传订单实体ID，不将展示订单号当id |
| 商品/地址 | 收藏→回读→取消；上下架恢复；8月30日发布QA商品2094026061076062209/2094026660756676610后仅删除这两件，列表恢复7条；地址新建后仅删除本次并恢复原默认 | 商品逻辑删除不代表上传文件物理删除；完整多步发布/上传/菜单删除及异常未全验 |
| 求购 | 8月30日QA求购2094032284357844993/2094032551400792066创建→核对→取消，后者页面/新实例恢复通过，均无关联订单 | QA三级分类根2094031986071527426已停用，可按后续授权恢复；取消记录/上传保留，不动历史求购 |
| 仅退款/评价 | 仅退款创建/撤销回读；隔离评价回复/申诉/删除；8月30日评价2094037533512523778创建/恢复/卖方回读后删除 | 删除不恢复评价资格；同意/驳回、同键退款重试、真实异常未全验 |
| KYC/IM媒体 | 8月29日三张明确非真实证件PNG上传/签名/PENDING；合成1秒M4A上传、发送、公开资源GET与H5播放入口；此前双账号文本实时到达/已读/撤回 | KYC私有资源未通过；同源标签不等于独立双账号；新schema/新媒体撤回/断线仍独立验收 |
| 物流/消息 | 隔离履约订单正常轨迹经买手/顾客API精确回读；9月QA会话2093370220886970369的新文本2096198819033735169已撤回 | 10条历史撤回仍含原文，前端只显示占位；未擅自清库/改签名Host |
| 公共页面 | 8月50路由访客检查与受保护直达守卫、状态页签/失败态；9月规定设备模式新展示结果见下节 | 历史空态/DOM/构建不替代最新点击、写入或真机验收 |

### 最新 Chrome 展示验收与恢复点

本次独立补验（基线e985250）：第一时段标签1281847290的插件输入派发超时后已释放；第二时段标签1281847303复用原登录，原生DevTools确认iPhone15Pro393×852/Fit，使用同Chrome新鲜截图坐标完成下表补验。原有H5/PC/后台标签未操作，未切账号或提交业务，已关闭自有标签并释放浏览器。实际流水已包含9月6日记录，旧39条不再作为本次基线。

源码补充：积分LedgerFilters原受log页签v-if控制，切规则/申诉会销毁草稿/已生效标志，但父filters保留；已改v-show保留实例及身份key，并实际验证加分筛选跨两个页签返回仍保留。钱包首页总资产在393宽真实裁切，已仅对格式化长度超过12的金额使用60rpx，其余维持88rpx，并允许超长换行、单位不压缩；刷新后U 1,100,001,069.00完整可见，不改计算或余额来源。两修复已通过类型、H5/App构建与差异检查，不代表真机验收。

首页/协议证据保留自本批先前标签1281847277；此前仅恢复本仓5174开发服务，未重启他端。钱包/积分/求购/分类以下使用第二时段新点击证据，未用内置浏览器。日期选择器首次展开曾出现无样式年份列表；整页刷新后取消、开始/结束日期确认及多次展开未再复现，根因未定，未添加隐藏样式或记作已修复。

| 范围 | 本批实际通过/观察 | 未验与恢复步骤 |
|---|---|---|
| 首页/我的/仪表盘 | 首页 AI 入口 DOM 无匹配、我的截图无入口；Hero→KPI→经营统计层级可见，真实 12.5% / 33.33% / 0.87小时、12卖出/6在售保留，保证金仍为 — | 不涉及商品详情浏览或交易；其它统计样本不扩展验收 |
| 帮助协议 | 用户服务协议打开，v2.0.0/生效日期/原正文及演示声明保留，遮罩关闭已确认，页面返回回到仪表盘 | 本批仅点击该一份协议，其余四份沿用历史，不算新点击通过 |
| 流水/总资产 | 从我的→钱包→流水；展开→业务号2086341303743373314→应用收起→单条十亿金额→详情，完整流水号2086341304070529025/备注/余额可见；关闭×通过，实际返回钱包通过，再入后默认条件，重新应用并重置恢复多条。首页修复后整页刷新完整显示总资产 | 返回钱包后再次进入是新流水页面，恢复默认条件，不误记为跨卸载保留。深分页仍无第二页样本；大金额证据为现有测试流水，不是本批充值 |
| 积分 | 加分→应用收起→规则返回→申诉返回→展开仍为加分/已生效；日期取消不改值，开始/结束均选9月6日后筛选为空；重置收起恢复原KYC+100及8月9日23:32:22；页头返回正常 | 无真实扣分及深分页样本；日期首次无样式异常刷新后未再现、根因未定；本批不新增切账号证据 |
| 求购/分类 | 我的预算上下限1、交期上下限7→收起命中2条→详情2094032551400792066/进度→实际返回保留四条件→重置6条。大厅展开应用/收起/重置均为空态。创建页重试出现加载后仍不可选，点击分类不打开空选择层，提交保留is-disabled；商品发布分类弹层只有QA测试且选择回填正常 | 未点击提交或创建数据；大厅非空/抢单未验。求购无启用三级链，商品空分类分支无样本。新增求购会话清理按身份key仅源码/构建确认，因同源会影响原登录，本批跳过账号切换 |
| 理财 | H5本轮加载完成暂无产品；据主协调转述，PC正式/finance同为空态，另次独立QA只读API确认products/list HTTP200/code1数组0项、收益概览与FINANCE_LOCKED桶为合法零 | PC另次API不是本端响应或页面同次抓包；缺在售隔离产品，申购等资金写链未验 |

本轮只读点击清单已收口至实际样本边界；原锁屏和首次派发超时不再列为当前永久阻塞。第二时段末页为钱包，关闭自有标签前无弹层，原账号未变，控制台error/warn读取为空。未执行商品browse、消息已读、订单/资金/审核/配置或删除。仍缺新补丁账号隔离、深分页、扣分、大厅非空及完整三级分类条件，不以旧证据替代；代码与文档交付状态见下方。


## 未完成与后端补充清单

证据均标明历史或本次来源，失败原因未定位不等同缺接口；各端数据不互相冒充验收。

| 编号 / 优先级 | 页面与问题证据 | 归属 / 当前状态 | 前端处理与恢复验收条件 |
|---|---|---|---|
| KYC-01 / P1 | 第二批两明确QA身份资料均PASSED，当前schema v6：两证件类型、背面/手持必填、允许驳回重提；详情仍HTTP200/code=-1“文件不存在或已删除”。历史签名地址问题不以本批配置读取关闭 | 后端文件/存储缺口仍在；另发现前端startAnother无模板入口、verified回执永久锁住已允许重提表单的独立缺陷，已最小接回“重新申请” | 仅verified、成功读取同记录且规则允许驳回/过期重提时开放，仍走原startNextKyc严格回查，不因文件失败解锁或自动清回执。24项源码内存分支通过；Chrome QA-A页面读取确认保留已通过与历史资料不可查看，无重新申请/上传/提交入口。暂无可读驳回/过期+本机verified样本，实际重申请/新材料闭环未验 |
| QA-01 / P1 | 买手驳回后重提及其他后台状态分支缺完整样本 | 联调/测试条件，不是接口缺失 | 具备隔离数据后按提交→响应→刷新→跨端回读验收；评价回复/申诉/删除、通知删除/清空原证据保留 |
| FUND-01 / P1 | 充值地址失败提示缺 `chain.callback.base_url`，充值创建返回 -1 | 后端部署配置；历史实测未闭环，本批未重新请求 | 页面保留普通失败/重试，完整配置诊断仅记此处；后端修复配置后验地址、创建与状态回读，不伪造到账 |
| FUND-02 / P1 | 提现、提前赎回及异常防重缺可控写成功条件；本批H5加载完成“暂无小金库产品”。据主协调转述，PC正式/finance同为空态，另次独立QA只读API确认products/list HTTP200/code1数组0项，收益概览与wallet.distribution的FINANCE_LOCKED桶均为合法零；非页面同次抓包，旧/finance/products错误详情入口不作列表失败证据 | 两端空态一致；PC另次API证据不冒充本端响应或资金闭环，仍缺在售隔离产品与可控写条件 | 保留真实空态、普通失败/重试、未知金额、确认和同键防重；有样本后验写入/失败/资金回读，不生成资金测试数据 |
| ORDER-01 / P2 | 第二批双方本人列表确认内部orderId2093369377466966017（展示号2093369377458577408）COMPLETED/SIGNED，物流采购/发货凭证各1、3轨迹，两个凭证资源GET均200 image/png；2093370220769533954为SHIPPED/IN_TRANSIT、4轨迹、凭证空。详情/物流双方均200/code1 | Chrome iPhone15Pro393×852/Fit的QA-A页面确认凭证图片完整加载、签收3轨迹；运输中订单刷新后4轨迹与两个操作入口可读。轨迹弹层插件点击超时，原生截图随后不可用，弹层开关未验；展示号误作id已核正为测试参数问题 | 无新发货/物流写入；完整售后/费用及异常写仍未验。H5订单商品区仅展示，productId=null不生成商品跳转，未照搬PC整改 |
| PRODUCT-01 / 已核正 | 本批定向重读 swagger-config/order：存在 DELETE /products/delete，必填 query id 为 int64，成功响应 ApiResultLong；H5 deleteProduct 已封装且商品列表/详情均调用 | 非后端缺口、非 H5 未接入；旧表结论错误已纠正。本批未执行真实删除，不补记写验收 | 保留原按归属/状态显示的真实删除按钮、删除确认及互斥保护；本批不新增删除能力、不实际删除。求购进度/经营统计已读取，不再列 user 恢复待办 |
| CMS-01 / 延期 | 公告、帮助文章、正式协议服务无本期适用 C 端契约 | 后端/产品延期，未完成 | 公告旧路由仅暂不可用/返回，帮助不显示缺接口说明或假文章；保留五类协议正文/版本/同意流程及必要演示声明。正式契约和内容获准后恢复 |
| AI-01 / 延期 | AI 导购无本期适用 C 端契约 | 后端/产品延期，未完成 | 首页/我的移除无效入口，旧路由简短返回页；不展示模拟推荐。需真实对话/推荐契约及权限后重新接入 |
| AML-01 / 延期 | AML 能力不纳入本期 | 后端/产品延期，未完成 | 不新增功能入口，现有法律文本与同意流程不动；不得将移除“本期延期”研发前缀算业务验收 |
| CATEGORY-01 / P2 | 本次只读live分类：onlyEnabled=true仅1个一级节点2084936769859051521；false共4节点，唯一三级2094032162416844802位于停用根2094031986071527426下。Swagger求购明确三级ID，商品仅声明分类ID；两页分别筛三级/展开全部启用节点 | 测试配置导致求购0选项、商品1选项，非前端漏筛；两个真实响应经生产筛选函数均为0可用三级。Chrome求购重试/空选择拦截与商品QA测试选择回填已验 | 保留三级/祖先禁用/提交前重读，未改配置或提交；求购按钮is-disabled仅只读核对，未实际点击提交。完整启用三级链恢复后再验非空选择与创建，商品空分类分支仍缺样本 |
| IM-01 / P1 | 前批 10 条历史已撤回 QA 消息仍保留原文，新文本撤回已清空 | 后端历史数据处理；本批未复测且不删记录 | 前端保持撤回占位/媒体停止和显式已读；后端明确历史治理方案后回读历史结果，图片/语音撤回、可见已读及重连另作受控专项，本批不进入会触发已读的会话 |
| QA-02 / P2 | 钱包前批 39 条少于每页 50，积分无真实扣分样本；顺序账号切换已有通过证据 | 样本/专项条件，不是已确认后端缺陷 | 不造数据，不以首屏/构建通过代替深分页、扣分、异常竞态验收；新折叠筛选/返回/长金额/分类空态按本轮实际样本记录，缺样本继续保留未验 |

补充原独立报告仍有效的范围：KYC需明确审核文件保留策略、可达HTTPS签名、有效期内历史详情持续可读；充值8月曾报 Empty key，9月报缺 chain.callback.base_url，开放链应有可用地址/最低额配置。现有仅退款不等于退货、维修、换货、保修完整工单；若产品要求这些能力，需补证据/寄回物流/历史/取消契约，否则明确本期仅退款范围。上述都未因文档清理关闭。

受控回归恢复标准：提现覆盖 REVIEWING→APPROVED→SUCCESS/REJECTED（禁止真实链上出款）；保证金用有可用余额且无在途冻结的隔离买手核对缴纳/同键/退还；赎回需 HOLDING 且 canRedeem 的隔离单；买手申请及求购需驳回/重提/审核/推送/抢单状态条件；退款需 AGREED/REJECTED 两分支；物流用 SHIPPED 隔离单标记→双方回读→后台解除；IM需可控网关断开/恢复及断线期间消息。每项均按提交→明确响应→刷新→跨端状态/金额/流水回读验收，不由前端伪造。

## 验证、提交与下一步

- 本次全量待提交检查通过：pnpm typecheck、pnpm build:h5、pnpm build:app-plus、现有共享C端契约脚本及 git diff --check；新增求购隔离补丁后已重跑。当前源码 RequestError 7项临时进程检查通过；新增内容敏感模式扫描无命中，未混入本地配置/依赖/临时文件，顶部承接原文校验一致。生产构建不代表真机验收，历史临时断言不称本次全量测试；未新增测试文件/框架。本批检查通过；准确提交与远端同步状态以 Git HEAD 和远端分支为准，不代表部署或完整业务验收。
- 提交复核发现求购大厅/我的列表缺少会话变更后的筛选清理，已按现有钱包/积分模式补齐清空查询及按身份重建筛选组件；不改变查询字段和默认业务行为。该补丁仅做源码与构建检查，没有新增浏览器验收，不继承前批正常切换为新补丁点击通过。
- 2026-09-06 刷新 live swagger-config，复用现有 C-PC 只读契约检查脚本并指定本文源站，四组检查通过；未改脚本/他端文件，数量和字段范围见矩阵。普通请求失败不回退Mock；-311/-312不是登出码，诊断文案收敛不更改kind/code/statusCode。
- 本批文档整理合并旧日期流水、被新契约替代的“无幂等键/无统计/无进度/商品删除缺口”等结论；顶部跨设备承接原文不动。未跟踪 backend-remediation.md 的有效问题/流程条件已归入本表及矩阵，原件已在仓库外备份并逐字节比对一致后移除；恢复来源为 /tmp/c-h5-doc-cleanup.PAp7Bu/backend-remediation.md，不是Git，系统临时目录不保证长期保留。
- 第二时段新增积分筛选页签保留、钱包总资产长金额修复后已重跑pnpm typecheck/build:h5/build:app-plus和git diff --check通过；浏览器已释放。本批按用户“更新推送”授权将两页面与两文档一起交付main，准确提交和同步状态以Git与推送回执为准；不部署，不改顶部承接。整体仍部分完成，下一步在独立会话、深分页/扣分/大厅非空/启用三级链等条件满足后补专项，不造样本、不以构建替代验收。后台协作仍使用 https://testhou.merchantsale.store/，不启动本地后台。
- 默认保留真实UI流程、Long ID原值、精确金额、账号/原键/未知结果保护。部署H5走同源/api，App仅HTTPS；真机仍未验，CMS/AML/AI与正式汇率按既定延期边界。
- 第二批仅修复KYC已允许重提的原业务入口，改码后24项当前源码内存分支、pnpm typecheck/build:h5/build:app-plus通过；本次提交前确认源码未另改并复用上述结果，git diff --check再次通过。Chrome只读补验边界见ORDER-01/KYC-01，自有标签1281847311已关闭并释放，未切换原QA-A。按用户新授权“都推送代码吧”，本次将KYC页面与两份文档共3文件提交推送main；准确提交和远端同步以Git回执为准。未新增测试文件/脚本/依赖或业务写入，不部署；顶部承接、README、AGENTS不变，KYC重申请闭环及物流弹层开关仍未验。
